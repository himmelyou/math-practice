# Jarvis Math Lab · 公开发布前安全与部署清单

> **用途**：在将本程序面向公网推广（GitHub Pages 主站 + Render 后端等）**之前**，按本文逐项核对并打勾。  
> **关系**：实现细节与历史状态见根目录《安全加固与实施状态.md》；本文在其基础上合并了发布架构、公开面风险与运维项，并更新至当前代码（含错题按需 API、`sync`/`patch` 响应等）。  
> **维护**：发版或安全相关改动后，请同步更新本文「已完成能力」与检查项。

---

## 一、先建立正确预期（避免误判）

### 1.1 Private 仓库 ≠ 网站私有

| 层级 | 是否对公众可见 | 说明 |
|------|----------------|------|
| **GitHub 私有仓库** | 否（无权限者不可浏览源码） | 需相应套餐才支持「私有仓 + Pages」；协作者可见全部代码。 |
| **GitHub Pages 已发布文件** | **是** | 任何人拿到 URL 即可打开页面，并在「查看源代码 / Network」中下载 **已发布的静态文件**（HTML / JS / CSS / 图片）。 |
| **`server/` 后端** | 否（不随 Pages 发布） | 运行在 Render 等；密钥与 `users.json` / `runs.json` 在服务器数据目录。 |

**结论**：安全应依赖 **HTTPS + API 鉴权 + 强密钥**，不能依赖「仓库设为 private」或「不告诉别人链接」。

### 1.2 若 Pages 源目录为整个 `docs/`（当前项目惯例）

下列内容会随 Pages **对访问者公开**（`noindex` 仅影响搜索引擎，不防直接访问）：

- 学员主站：`index.html`、`config.js`（含 API 域名）、`shared/expand-brackets-questions.js`（出题逻辑）、各 `stats-*.js` 等  
- **管理端**：`docs/admin/` 全部静态资源  
- **学员报表页**：`docs/report/` 全部静态资源  

下列内容**不会**出现在 Pages 上（仍在私仓 / 服务器）：

- `server/server.js`、环境变量、`DATA_DIR` 下 JSON 数据  
- 仓库根目录说明文档（如 `拆括号等级说明.md`），除非单独拷进 `docs/`

### 1.3 对「问题大不大」的简要结论

- **学员账号、密码、错题、对局记录**：经 JWT + `ensureOwnData` 保护，**不写在静态页里**；主线合理。  
- **主要剩余风险**：管理口令强度、管理页是否公网可访问、登录/管理接口**无限流**、CORS 较宽、JSON 文件存储的运维与备份。  
- **前端 JS 可见**（含出题算法）：教育类产品常见，一般**可接受**；防作弊需服务端出题（成本高，非发布阻塞项）。

---

## 二、发布前必做（阻塞项）

发版前请逐项确认，**全部完成**再对外推广。

### 2.1 Render / 后端

- [ ] 已部署最新 `server/`（与前端 `?v=` 缓存版本匹配；API 行为变更须前后端同发，如错题 `wrong-answers`、`POST /runs` 的 `sync`、`PUT` 的 `patch`）。  
- [ ] 已设置 **`JWT_SECRET`**：随机长字符串；**禁止**留空或使用可猜默认值（未设置时服务应无法启动）。  
- [ ] 已设置 **`ADMIN_PIN`**：足够长、不可猜测（建议 ≥12 位，字母数字混合）；仅通过安全渠道交给教师/运营。  
- [ ] 已配置 **`DATA_DIR` 持久化**（Render 磁盘或挂载卷）；`users.json`、`runs.json`、`settings.json` 等重启不丢。  
- [ ] 生产环境 **`NODE_ENV=production`** 或存在 **`RENDER`**，使 Cookie 为 `secure` + `sameSite: none`（跨子域 Pages ↔ API 时需要）。  
- [ ] API 对外仅 **HTTPS**（如 `https://api.adsmathlab.com`）。  
- [ ] **发版前备份** `DATA_DIR` 下 JSON（尤其 `users.json`、`runs.json`）；《迁移功能清单》提醒：**勿随意更换 `JWT_SECRET` / 数据目录路径**，否则全员需重新登录或数据路径错乱。

### 2.2 GitHub Pages / 前端

- [ ] Pages 已启用 HTTPS（自定义域名亦需有效证书）。  
- [ ] `docs/config.js` 中线上环境指向**正确**的 `API_BASE_URL`（与 Render 一致）。  
- [ ] `docs/shared/app-version.js` 中 **`JML_CACHE_TAG`** 与各 HTML 内 `?v=` **一致**；发布后提醒用户 **Ctrl+F5** 强刷。  
- [ ] 确认发布目录：若整包发布 `docs/`，则知悉 **`/admin/`、`/report/`  URL 可被直接访问**（见第三节可选加固）。

### 2.3 密钥与仓库

- [ ] 仓库内**无**明文 `JWT_SECRET`、`ADMIN_PIN`、学员真实密码、生产数据文件提交。  
- [ ] `.gitignore` 已排除本地 `server/data/`、敏感配置（按项目现有规则）。  
- [ ] 仅通过 Render 环境变量 / 平台密钥管理注入生产密钥。

### 2.4 最小功能自测（安全相关）

- [ ] 学员 A 登录后，无法用 API 读取学员 B 的 `GET /api/user/B`、错题、`runs`（应 403）。  
- [ ] 未登录访问上述接口应 **401**，前端回到登录页。  
- [ ] 注销后 `auth_token` Cookie 清除，刷新不可继续操作。  
- [ ] 错误 `X-Admin-Pin` 无法调用 `/api/admin/*`。  
- [ ] 学员注册/登录、记一局、错题本打开/清空、训练进局、数据统计页可正常使用（前后端版本一致）。

---

## 三、强烈建议（发布前或发布后短期内完成）

### 3.1 管理端暴露面

- [ ] **评估**是否将 `docs/admin/`、`docs/report/` 与学员主站**同域发布**。  
  - 若同域发布：务必保证 **ADMIN_PIN 足够强**，并考虑第三节限流。  
  - **更稳妥**：Pages 仅发布学员所需子集；管理端改内网、独立子域 + 访问控制（Cloudflare Access 等），或仅本地 `local-admin-server` + 正式环境 Render API。  
- [ ] 主站内嵌「难度配置」同步依赖 `sessionStorage` 的 `jml_admin_pin_v2`（与 `admin-auth.js` 一致），**无**写死口令；教师需先通过管理口令门再改设置。  
- [ ] 定期轮换 `ADMIN_PIN`（`PUT /api/admin/pin` 需旧口令；紧急可用环境变量 `POST /api/admin/pin/reset` 流程，见后端实现）。

### 3.2 防暴力破解（原《安全加固》标为可选，公网发布建议提升优先级）

- [ ] 对 `POST /api/login` 按 IP 或用户名限流（例如 5～10 次/分钟）。  
- [ ] 对带 `X-Admin-Pin` 的管理接口限流，降低撞口令风险。  
- [ ] 可选：连续失败后临时锁定账号或递增延迟。

### 3.3 CORS 收紧（当前 `origin: true` 较宽）

- [ ] 将 `server/server.js` 中 CORS 改为**明确白名单**：你的 Pages 域名、自定义主站域名、本地开发 `http://localhost:*` / `127.0.0.1:*`。  
- [ ] 避免长期 `origin: true` + `credentials: true` 对任意第三方站点开放（降低跨站诱发已登录请求的风险面）。

### 3.4 备份与恢复

- [ ] 建立 **定期自动备份** `DATA_DIR`（Render  cron、外部脚本或手动规程）。  
- [ ] 文档化 **恢复步骤**（还原 JSON、重启服务、验证登录与积分）。  
- [ ] 重大发版前手动备份一份并留存版本标签。

### 3.5 合规与对外说明（《商业版完善指引》第四节）

- [ ] 准备 **隐私政策**、**用户协议**（说明收集的数据：账号、练习记录、错题等）。  
- [ ] 若面向未成年人，评估 **儿童个人信息** 相关合规要求（地区法规不同，需自行法务判断）。  
- [ ] 提供 **反馈/联系** 方式（邮箱或表单）。

---

## 四、当前已实现的安全能力（发版时默认具备）

以下在现有代码中**已实现**，发布时无需重复开发，但需在部署层**正确配置**。

### 4.1 学员账号与密码

- 密码 **bcrypt** 存储；旧明文密码登录后会自动升级。  
- 注册、登录、修改密码、管理员创建用户均走哈希校验。

### 4.2 学员会话与接口鉴权

- 登录/注册：JWT 写入 **HTTP-only** Cookie `auth_token`（7 天）。  
- `requireStudentAuth` + `ensureOwnData`：仅允许操作 **自己的** `username`。  
- 已保护的学员接口（均需登录且本人）：  
  - `GET/PUT /api/user/:username`  
  - `GET/POST /api/user/:username/runs`  
  - `GET/POST/DELETE /api/user/:username/wrong-answers`  
  - `POST /api/user/:username/expand-brackets-wrong-answers`  
  - `POST /api/user/:username/change-password`  
  - `POST /api/logout`  
- 学员 `GET/PUT user` 响应经 **`safeUserForStudent`**，不含 `expandBracketsWrongAnswers`（拆括号错题仅管理端报表使用）。  
- 前端 `fetch` 使用 `credentials: 'include'`；**401** 统一 `handleAuthRequired()` 回登录页。

### 4.3 管理端与学员端分离

- 学员：`GET /api/user/:username`  
- 管理：`GET /api/admin/user/:username` 等，需 **`X-Admin-Pin`**  
- 口令来源：`ADMIN_PIN` 环境变量 → `admin-pin.json`；皆空则管理接口拒绝

### 4.4 性能相关 API 设计（降低误用与负载）

- 错题本：`GET/POST/DELETE .../wrong-answers` 按需读写，打开错题本不必再拉全量 user。  
- 记局：`POST .../runs` 返回 **`sync`**（仅相关 `recent*Runs` 与积分等），非整包 user。  
- `PUT user` 返回 **`patch`**（仅本次修改字段）。  
- 训练进局、统计页：前端已做「先展示缓存/占位，后台再刷新」（不替代鉴权，但改善体验）。

---

## 五、可选 / 发布后迭代

| 项 | 说明 |
|----|------|
| 出题逻辑服务端化 | 防止从前端 JS 完全还原题目；成本高，按商业需要排期。 |
| 多租户 / 机构隔离 | 《商业版完善指引》长期方向；当前为单库 JSON。 |
| 监控与告警 | Render 健康检查、错误日志、磁盘用量。 |
| WAF / CDN | 在 Pages 或 API 前增加 DDoS、Bot 防护。 |
| 学员密码复杂度策略 | 注册时强制最少 6 位等（后端部分已有校验，可加强提示）。 |
| 审计日志 | 管理端查看/导出学员数据的操作记录。 |

---

## 六、环境变量速查

| 变量 | 必填 | 说明 |
|------|------|------|
| `JWT_SECRET` | **是** | 学员 JWT 签名；随机长串；泄露等于可伪造登录态。 |
| `ADMIN_PIN` | **强烈建议** | 管理接口口令；与 `admin-pin.json` 二选一优先 env。 |
| `DATA_DIR` | 建议 | 持久数据目录；默认 `~/.jarvis-math-lab/data` 类路径。 |
| `NODE_ENV` / `RENDER` | 建议 | 生产 Cookie 安全属性。 |
| `PORT` | 按平台 | Render 常自动注入。 |
| `COHORT_STATS_TTL_MS` 等 | 否 | 统计常模缓存，见 `server.js`。 |

---

## 七、推荐部署架构（与《产品简介》一致）

```
学员浏览器
    → HTTPS → GitHub Pages（docs/ 静态资源）
    → HTTPS → Render API（server/，JWT Cookie）
    → 持久卷 DATA_DIR（users.json, runs.json, settings.json, …）
```

- **不要**把 `server/data/` 或含真实学员数据的 JSON 提交到 Git。  
- **不要**在未改 `config.js` 的情况下把 Pages 指到错误 API。  
- **前后端同发**：尤其涉及 API 契约变更时，避免旧前端对新后端或反之。

---

## 八、发布当日操作顺序（建议）

1. 备份 `DATA_DIR` 全量 JSON。  
2. 部署 Render：确认环境变量 → 健康检查 `/` 或现有探活 →  smoke test 登录。  
3. 部署 Pages：确认 `JML_CACHE_TAG` / `?v=` 已 bump → 打开主站强刷。  
4. 执行第二节「最小功能自测」。  
5. 用**非管理员**账号在公网环境走一遍：注册 → 玩一局 → 错题本 → 退出。  
6. 用管理员口令仅在自己环境打开 `/admin/` 或本地管理入口，确认报表可读。  
7. 对外公布主站 URL；**不必**公开管理端 URL；口令仅私下分发。

---

## 九、发版后持续关注

- [ ] 定期检查 Render 磁盘与备份是否成功。  
- [ ] 关注异常登录或管理接口 403/401 日志（若平台提供）。  
- [ ] 学员量显著增加时，评估 JSON 单文件读写性能与拆分存储（架构债）。  
- [ ] 安全相关代码合并后，更新《安全加固与实施状态.md》**与本文**对应章节。

---

## 十、相关文档

| 文档 | 用途 |
|------|------|
| 《安全加固与实施状态.md》 | 实现状态与待办的历史记录 |
| 《商业版完善指引.md》 | 产品/合规/商业化长期项 |
| 《迁移功能清单.md》 | 发布前功能自测与部署注意 |
| `server/README.md` | 后端本地运行与部署简述 |

---

*文档版本：2026-05-25 · 整合公开发布安全评估与《安全加固与实施状态》及当前代码（错题 API、sync/patch、Pages 公开面说明）。*
