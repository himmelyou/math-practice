# 练习建议接入主程序 · 开发清单

按条讨论、按条落地。引擎规则先按《练习建议规则说明》改；不要改训练 F/H，除非某一条明确要改。

## 已做

- [x] **0. 入口款式**  
  首页头像右侧：上面设置、下面「每日任务」。仅登录可见，游客隐藏。中英/繁双语。

- [x] **1. 任务列表页**  
  登录学员点「每日任务」进独立页：未完成一行一张（标题 + 目标 + 进度，无原因）；每条右边胶囊「开始挑战」，任务 1 描边表示推荐。全部历史可折叠。热图全空只排闯关（过 L1）。游客隐藏入口。

- [x] **冷却窗不清空（v0.16）**  
  规则升级、重新开单、跑偏、热图变都只重算未完成，已完成冷却窗保留（满 5 挤最早，日历过期不加回）。report 点「重新开单」不再整单丢弃。昨晚 0.14→0.15 把冷却一起扔掉的 bug 在本步修掉。

- [x] **放弃局不算 + 均速只算对（v0.17）**  
  中途放弃不进成功/失败/碰巧完成；热图仍计入已答题。本局均速、折线、回填、任务窗均速与热图对齐，只算答对且 ≤1 分钟。

- [x] **失败进冷却（v0.19）**  
  队头失败从未完成拿走，与成功一起进冷却窗（合计最多 5，满则挤最早），不后插。间隔仍按冷却+未完成一条队列。永久历史只记成功。report 第二行改名「冷却」，失败方块画出来。

- [x] **不整单重排未完成（v0.20）**  
  积压局先对旧五格跟完，规则升级只改版本号不拆队。任务局成功/失败只补一格，剩下四格不动。名单外才重算未完成，已有尝试记录的关保留。点「重新开单」仍丢掉未完成再按热图重排。

- [x] **五条都能开练（v0.21）**  
  未完成 1～5 右侧胶囊「开始挑战」。对上哪条跟哪条（记尝试/成功/失败），点按钮不把该条抽到队头。首页打中名单里的关也跟局；当场成功称碰巧完成。失败仍进冷却、只补一格。

- [x] **本局判定 + 结算对照（v0.22）**  
  训练成功按这一局的准度/均速对照目标，不再多局平均、不再卡 2 局或 20 题。任务结算等跟局后再出卡：目标速度 / 该局速度，目标准确率 / 本局准确率。首页普通训练结算不动。

- [x] **2. 任务单服务器**  
  引擎只在服务器：`server/practice-advice.js`。按用户落盘未完成 1～5 + 冷却窗（成功+失败最多 5）+ 全量成功历史（`STORE_FULL_HISTORY = true`，只记成功，不记失败/尝试；升级/重新开单不清；无清空按钮）。H5 / report 只渲染接口。report 五格冷却窗含失败。

- [x] **3. 算单年级**  
  服务器读用户资料里的年级，学员端仍不下发年级。

- [x] **4. 任务 1「开始挑战」接线**  
  只有任务 1 有描边（推荐）。每条未完成都有「开始挑战」。训练/开新关跳过 F/H，钉死该关，`trainingMeta.dailyTask` 打标签；不改首页训练选关。闯关/冲榜进现有闯关、从 L1 开。本会话「再试一次」仍钉同一关。局存盘后问服务器 `lastFollow`，**跟局到了再出结算卡**（避免先闪训练文案）：成功 / 碰巧完成 →「挑战成功」；失败 →「还需努力」；未达 →「还需努力」+ 再试一次。四格为目标准确率/速度对照本局。放弃仍用放弃标题。右上角「返回首页」保留。外面打的局仍打开任务页才跟（对上 1～5 则记尝试）。任务开局补拉热图（不改 F/H）；预览对照当前/目标准度与速度；纯准度任务目标速度为 `--`；开新关四项均为 `--`。

- [x] **5. 打完怎么回来**  
  已并入第 4 步。首页普通训练的「再玩一局」仍走 F/H。

- [x] **6. 任务页双语**  
  页标题、任务 n、模式名、折叠区、结算四句、开局对照已套 i18n。目标和进度由学员端按 `successKind` / 窗内准度均速拼，不再直接渲染引擎中文。report 五格仍用中文。

- [x] **7. 正式上线**  
  全体登录学员可进每日任务。全量成功历史接通。管理端备份/恢复纳入 `practicePlans`（schema v3；旧备份无此字段则不覆盖现有任务单）。report 打开为只读快照，点「刷新」才跟局，「重新开单」只重排未完成。

## 备注（2026-09-05 上线）

**何时算单**  
打完一局只写 `runs`。任务单在打练习建议接口时算并写回。水位 `lastProcessedTs` 只跟这之后的新局。第一次开单把水位设成开单时刻，更早的局不算完成。规则升级时必须**先跟旧五格再改版本号**，不能先拆队。

**任务局 vs 外面打的局**  
从某一条的「开始挑战」开的局，存盘后先跟局再出结算卡，只更新这一条；成功/失败才补一格。首页/其它入口打的局仍攒着，打开每日任务才跟；对上 1～5 则记尝试（本局达标称碰巧完成）。

**report**  
观察者：打开、刷新浏览器都不改单。任务单上的「刷新」= 把积压新局跟进当前单；「重新开单」= 丢掉未完成再按热图重排。学员打开每日任务、任务局结算仍会算单写回。

**全量历史**  
只记成功。冷却窗照常供对照。升级/重新开单不清全量表。
