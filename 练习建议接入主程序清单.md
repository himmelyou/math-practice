# 练习建议接入主程序 · 开发清单

按条讨论、按条落地。引擎规则（队头失败、2～5 碰巧完成、已完成窗口 5、同关隔 5）先不动，除非某一条明确要改规则。

## 已做

- [x] **0. 入口款式**（本步）  
  首页头像右侧：上面设置、下面「每日任务」。仅登录可见，游客隐藏。中英/繁双语。点进去还没有任务页。

## 待讨论（建议顺序）

- [ ] **1. 任务列表页**  
  点入口进入独立页。上面未完成 5 张卡片（一行一张，文案写全：如「任务 1 · 四则训练 L1」+ 目标 + 已试局数 + 原因）。下面已完成可折叠，带总数。返回首页。

- [ ] **2. 展示历史 vs 引擎窗口**  
  引擎仍只留最近 5 条已完成做间隔/淡出。另存一份只用于展示的 `history`（成功/失败都记，尽量保留全部）。两套不要混表。和 report 共用本机任务单键。

- [ ] **3. 主程序算单所需信号**  
  学员端目前不下发年级，助手会按「年级未填」排，和 report 可能不一致。是否登录用户下发自己的年级。任务页要等四则热图和近局到齐再开单。

- [ ] **4. 任务 1「开始挑战」**  
  只给队头按钮。训练/开新关：跳过自动选关，钉死该关，写进 `trainingMeta`。闯关/冲榜：进现有闯关（从 L1 开）。2～5 不放开始（仍可碰巧完成）。

- [ ] **5. 打完怎么回来**  
  结算后回到任务页并同步进度。助手会话里「再玩一局」是否继续钉关。返回任务 vs 返回首页。

- [ ] **6. 任务页双语**  
  卡片标题、目标、进度、折叠区文案。

- [ ] **7. 后做**  
  任务单服务端持久化（换设备）。report 五格对照可先保留。

讨论时一次只圈一条编号即可。
