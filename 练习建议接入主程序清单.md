# 练习建议接入主程序 · 开发清单

按条讨论、按条落地。引擎规则（队头失败、2～5 碰巧完成、已完成窗口 5、同关隔 5）先不动，除非某一条明确要改规则。

## 已做

- [x] **0. 入口款式**  
  首页头像右侧：上面设置、下面「每日任务」。仅登录可见，游客隐藏。中英/繁双语。

- [x] **1. 任务列表页**  
  测试员点「每日任务」进独立页：未完成一行一张（标题 + 目标 + 进度，无原因）；任务 1 描边 +「开始挑战」（本步不进对局）。全部历史可折叠。非测试员按钮文案仍是「每日任务」，点击 toast「开发中」。热图全空只排闯关（过 L1）。

- [x] **冷却窗不清空（v0.16）**  
  规则升级、重新开单、跑偏、热图变都只重算未完成，已完成冷却窗保留（满 5 挤最早，日历过期不加回）。report 点「重新开单」不再整单丢弃。昨晚 0.14→0.15 把冷却一起扔掉的 bug 在本步修掉。

- [x] **放弃局不算 + 均速只算对（v0.17）**  
  中途放弃不进成功/失败/碰巧完成；热图仍计入已答题。本局均速、折线、回填、任务窗均速与热图对齐，只算答对且 ≤1 分钟。

- [x] **速度任务须准≥95%（v0.18）**  
  速度小步成功：任务窗均速达标且准度 ≥95%。准度任务不卡速度。

- [x] **2. 任务单服务器（全量历史暂不接通）**  
  引擎只在服务器：`server/practice-advice.js`。按用户落盘的是**未完成 1～5 + 冷却窗（成功最多 5）**。H5 / report 只渲染接口。测试期误打误撞的完成只进冷却窗，**不写全量历史**（`STORE_FULL_HISTORY = false`）。全量表等正式上线后再接通：只记成功，不记失败/尝试；升级/重新开单不清；无清空按钮。report 五格仍是冷却窗。

- [x] **3. 算单年级**  
  服务器读用户资料里的年级，学员端仍不下发年级。

## 待讨论（建议顺序）

- [ ] **4. 任务 1「开始挑战」接线**  
  样式已在。训练/开新关：跳过自动选关，钉死该关，写进 `trainingMeta`。闯关/冲榜：进现有闯关（从 L1 开）。2～5 不放开始（仍可碰巧完成）。

- [ ] **5. 打完怎么回来**  
  从任务列表开的局：结算后回到任务页并再打接口，跟刚打的那局（完成/失败/碰巧/跑偏都在这一刻入账）。外面打的局先积着，学员不进「每日任务」则学员端不刷新。助手会话里「再玩一局」是否继续钉关。返回任务 vs 返回首页。

- [ ] **6. 任务页双语**  
  页标题、任务 n、模式名、折叠区、开发中 toast 已套 i18n。目标和进度仍是引擎中文写死，之后再译。

- [ ] **7. 后做**  
  备份是否纳入 practice-plans。全量历史落盘（上线接通）。report 打开改为只读快照、手动「刷新」才跟局（见下方备注；**暂不落地**）。

讨论时一次只圈一条编号即可。

## 备注（测试阶段，2026-09-04）

**何时算单（不是局后立刻算）**  
打完一局只写 `runs`。任务单只在打练习建议接口时算并写回：测试员打开每日任务、助手打开该学员 report、点「重新开单」。单上有水位 `lastProcessedTs`，只跟这之后的新局，不是全历史重判。第一次开单把水位设成开单时刻，更早的局不算完成。

**测试期怎么验完成**  
一般学员进不了每日任务。report 一点开就会算单写回，所以现在打开 report 才能看到误打误撞的完成、冷却窗刷新、新任务插进来。学员自己练当场不会完成。

**正式上线后的意图（先记着，先不改代码）**  
- 从任务列表开局 → 打完回任务页 → 触发重新计算。  
- 在外面打：可能碰巧完成，但只要不打开「每日任务」，学员端列表不刷新。  
- report 是观察者，**打开不要自动跟局**，除非手动按「刷新」。浏览器刷新页面也不该改单。「刷新」= 把积压新局跟进当前单；「重新开单」= 丢掉未完成再按热图重排，不是观察动作。测试期仍保持打开即同步。

**全量历史**  
测试期不存。冷却窗照常供对照。上线后再把 `STORE_FULL_HISTORY` 打开。已写进盘里的 `plan.history` 下次算单会清掉，不接着攒。
