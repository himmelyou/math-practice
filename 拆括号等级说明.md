# 拆括号模式 · 等级说明

> 本文为助手根据你已有草稿与讨论**整理的一版完整表述**，便于你对照修改；定稿后可与 `docs/index.html` 中生成器逐项对齐。  
> **错因说明**：根目录另有作者稿《新拆括号等级说明.md》（仅作底稿，**不随仓库自动改**）；各档「错因模板」以下文为准，已与该稿对齐整合。

---

## 全局约定（五档共通）

1. **字母**：不含字母的档位不出现字母。**L3、L4、L5** 允许在 **`a`、`b`、`x`、`y`** 中**自由选用**（同一题里**可以出现多个不同字母**，并与整数混写）：`a、b` 表示**常数项**，`x、y` 表示**未知数**；**并非**「整题只能选一个字母」。**除 L3、L4、L5 外**，题目里出现字母时**仅使用 `x`**。
2. **次数与乘积书写**：
   - **（1）次数与题型**：出现未知数时以**一次式**为主（整式中**不出现**单独的 \(x²\)、\(y²\) 及更高次项；**L3** 两段括号内仅「两项用 `±` 连接」，不产生未知数的乘积项）。**L5** **不适用**「一次式」限制（见 **L5** 专条：两括号交叉相乘可出现 **`xy`** 等项，仅作分配训练）。**纯数式**档位（如 L2）按该档「出题逻辑」可含乘、除，不要求写成「一次式」。
   - **（2）乘法与乘积写法**（题面、标准答案与选项一致）：**数与数**相乘用**乘号**连接（如 `3×4`）。**相同字母**相乘写成**幂**，如 `x×x` 写作 **`x²`**（各档是否允许出现该项仍以（1）为准；若某档答案中不应出现二次项，则出题上不产生需写 `x²` 的结构即可）。**常数与字母**相乘：常数写在字母**前**，**省略乘号**（如 `3×y` 写作 `3y`）；系数为 **1** 时写作 **`x`、`y`**，不写 **`1x`、`1y`**。**不同字母**相乘：**省略乘号**，字母按**拉丁字母表顺序**排列（本模式允许的字母为 **`a`、`b`、`x`、`y`**，故如 `x×y` 写作 `xy`，`x×a` 写作 **`ax`**）。
3. **答案形式**：**只做一次展开**（去括号 / 分配完毕即停）；**不要合并同类项**；**不要**把式子**算成最终得数**，**不要**把字母**代成数**（**L2** 见该档「答案形式（L2）」；**L3、L4、L5** 见各档）。若展开后出现多个含未知数的项，标准答案按规定的**项顺序**书写（见各档说明），四选一须与此一致。
4. **数字**：未特别说明的档位：**正整数**，范围在各档单独约定。
5. **展示**：单项前的**符号**与现网一致（如 **`-x`、`-y`**，项间 **`+` / `−`**）。常数字母 **`a、b`** 及 **L3、L4** 的括号、项结构等与现网一致。**L5** 用**除号 `÷`** 书写除法，**不出现**分数形式（不用分数线 **`/`** 当作分数、不用竖式分数）。**两括号整体相乘**时，字母与字母、数与字母的紧凑写法遵循第 **2** 条（2），与现网一致。

---

## 错因模板（维护方式）

- **不再维护「全库」统一编号**；每一档在下方**单独列出**本档错因，便于增删与和实现对齐。
- **出题策略**：先从**当前题结构适用的错因**里取候选；若适用错因**多于 4 条**，则**随机抽取 4 条**；每条错因对应**一类固定的错误答案形态**（实现里一错因一端式，避免多条错因落到同一错误式）。
- 作者独立底稿见根目录《新拆括号等级说明.md》；与本文各档错因表冲突时**以本文为准**（底稿不自动同步）。

---

## L1｜一层括号 · 入门（合并原 L1 + 原 L2）

**定位**：整数-only；一层括号；括号外为 **`+` 或 `−`**，括号内两项。

**出题逻辑**

1. **抽题**：约 **80%** 出 `A − (B ± C)`（括号外为减，去括号后括号内**逐项变号**）；约 **20%** 出 `A + (B ± C)`（括号外为加，去括号后**符号不变**）。
2. **数字**：建议 **1～12**（若希望「+」外小题更轻，该 20% 可仍用 **1～9**）。
3. **核心**：以括号外为 **`−`** 为主，少量 **`+`** 巩固「不在该变号时乱变号」。

**错因模板（L1 专用）**

1. 括号外为「−」时：整体未按规则变号。
2. 括号外为「−」时：只变第一项，第二项未跟着变号。
3. 括号外为「−」时：只变第二项，第一项未跟着变号。
4. 括号外为「+」时：整体都变号。
5. 括号外为「+」时：无故改变第一项符号，第二项不变号。
6. 括号外为「+」时：无故改变第二项符号，第一项不变号。

---

## L2｜乘除去括号（括号内外仅乘除相连）

**定位**：整数-only；**仅一层括号**；**括号外**与**括号整体**之间**只能是乘法 `×` 或除法 `÷`**（不能是加减）。括号**整体**可在括号外数字 **`k` 的左侧或右侧**（「括号在前」或「括号在后」均可）。括号**内**恰有**两个正整数**，两数之间为 **`+`、`−`、`×`、`÷` 四者之一**。本档**无字母**。

**出题逻辑**

1. **结构**：括号内记为 `(A op B)`，其中 `A、B` 为正整数，`op ∈ {+, −, ×, ÷}`（随机）。括号外唯一数记为 `k`（正整数）。**仅**允许以下四类外形（实现可随机抽，占比自定）：
   - `k × (A op B)`、`k ÷ (A op B)`（**括号在右**，`k` 在左）；
   - `(A op B) × k`、`(A op B) ÷ k`（**括号在左**，`k` 在右）。
   括号与 `k` 之间**只有** `×` 或 `÷`，不会出现 `k ± (…)` 这类加减在括号外的情况。
2. **数字**：`A、B、k` 的范围建议 **1～12**（或与 L1 同档），由实现统一；若某类拆法要求**书写可整除**（如 `(A±B)÷k` 拆成两项之商），需限制取值，避免歧义。
3. **核心（与 L1 一致）**：**不要求把整题算成一个最终的数**；只要求完成**拆括号**（或允许的那一步**拆完后的规范书写**），答案以**式子**呈现，不合并、不把中间结果算成单个数（除非某题型规定照抄题面，见下「答案形式」）。

**答案形式（L2）**——**只拆括号，不计算**

1. **总原则**：与原先加减去括号档相同——**答案侧不做「算出得数」**；只做「**该不该拆、怎么拆**」：能分配则分配展开，能去括号则去括号写成**展开式**；**不**把括号内、括号外先算成一个数再写答案。
2. **符合乘法分配律**的情形：外层为 **`×`**、括号内为 **`+` 或 `−`**（含 `(A±B)×k` 与 `k×(A±B)`）：**用乘法分配律拆开**，写成「`k` 分别乘括号内两项再相加/减」的一步展开式（**不算乘积的最终数值**）。
3. **括号内为 `×` 或 `÷`**（内层也是乘除）：**不按**加减分配律拆成「两项之和」；**去括号**时去掉括号，写成与外层衔接的**连乘、连除**一步式，**不**先算括号内的数。  
   **变号（本档含义）**：当**外层是除法 `÷`**、**括号内是乘法或除法**时，拆括号后 **A 与 B 之间的 `×` / `÷` 要按规则写成与外除衔接的一行式**——这里的 **「变号」指乘、除运算符号（×、÷）该怎么写**，与 L1「加减去括号时每一项 **正负** 变号」不是同一套规则。此前文档若写「变形」，易误解成「随便改写成另一种样子」；**以本条「变号」为准**。
4. **括号和 `k` 之间是除法 `÷`** 时，要分**括号在前**还是**括号在后**：
   - **`(A op B) ÷ k`（括号在前）**：若 **`op` 为 `+` 或 `−`**，**符合除法对加减的分配**，应**用除法拆开**（写成两项分别 `÷k` 再相加/减的一步式，**不算商**）。若 **`op` 为 `×` 或 `÷`**，则按上条：去括号写成**唯一正确的**连乘除一行式（例如 `(A×B)÷k` 对应 `A×B÷k` 这类写法）；**错误**的拆法（如把乘法结构当成加法结构去「拆成两项再除以 k」）本来就不是合法答案，**不属于**「两种正确形式里选一种」。
   - **`k ÷ (A op B)`（括号在后）**：**正确答案 = 把原题式子照抄一遍**（保持 `k ÷ ( … )` 形式，不展开）。**干扰项**可做成「按**错误**的分配律强行展开」的假答案。

**说明**：曾有人把 `(A×B)÷k` 与「拆成两项分别 ÷k」对比——后者若指 **误用分配**（把内层乘当成内层加减去拆），那是**错解**，不是与正确式子并列的另一种「也对」的写法；产品里**只认一种正确展开**，不必再单独写「禁止另一种正确形式」。

**错因模板（L2 专用）**

1. 乘法分配时漏乘括号内第一项。
2. 乘法分配时漏乘括号内第二项。
3. 乘法分配时，当括号内符号为加号或减号时，错看原有符号，导致分配后第二项符号错误。
4. 对于 `k × (A op B)` 或 `(A op B) × k`，且 `op` 为加号或减号时，将中间乘号看成 `op` 进行分配。
5. 对于 `k × (A op B)` 或 `(A op B) × k`，且 `op` 为乘号时，将 `op` 看成加号导致误用加法分配律。
6. 对于 `k × (A ÷ B)` 或 `(A ÷ B) × k`，写成 `k × A ÷ k × B` 的错误分配。
7. 对于 `k ÷ (A × B)` 或 `(A × B) ÷ k`，写成 `k ÷ A × k ÷ B` 的错误分配。
8. 对于 `k ÷ (A ± B)` 时，对括号内强行分配所有项。
9. 对于 `k ÷ (A ± B)` 时，对括号内强行分配，且只分配部分项。
10. 对于 `k ÷ (A × B)` 或 `k ÷ (A ÷ B)`，对括号内强行分配成 `k÷A × k÷B` 或 `k÷A ÷ k÷B`。
11. 对于 `(A ± B) op k`，`op` 为乘或除时，只乘或除第一项，第二项照抄。
12. 对于 `(A ± B) op k`，`op` 为乘或除时，只乘或除第二项，第一项照抄。
13. 对于 `(A ± B) op k`，`op` 为乘或除时，只乘或除第一项，第二项漏抄。
14. 对于 `(A ± B) op k`，`op` 为乘或除时，只乘或除第二项，第一项漏抄。
15. 对于 `(A ± B) ÷ k`，分配后对于原括号内的符号分配错误（例如加号变减号等）。

---

## L3｜两个括号并排（无嵌套）· 数与字母（abxy）

**定位**：**运算规则**与原先 L3 相同：**两个括号并排**，中间为 **`+` 或 `−`**；**无括号嵌套**。**不再限制**「`A、B、C、D` 只能是正整数」：`A、B、C、D` 每个位置各自可为**整数**，或 **`a`、`b`、`x`、`y` 中的某一个**。**同一道题里可以混用多个字母与数字**（例如 **`(x + 3) − (y − b)`** 同时出现 `x、3、y、b`），**不是**「整题只能出现一种字母」。**`a、b`** 表示**常数项**；**`x、y`** 表示**未知数**。

**出题逻辑**

1. **结构**（不变）：左右各一段括号，中间为**段间**二元运算符 `+` 或 `−`。通式仍为 `(± A ± B) ± (± C ± D)`：从左到右依次为「左段」「段间」「右段」。**每一段**里：第一个 `±` 表示**该段首项**（`A` 或 `C`）取正或负；第二个 `±` 表示**该段两项之间**是加还是减。段间用 `+` 还是 `−`、各段内部两个 `±` 如何抽，由实现随机，占比可自定。
2. **四项 `A、B、C、D` 的取值**：每一格**独立**为**整数**（建议范围如 **1～15**，由实现统一），或 **`a`、`b`、`x`、`y` 之一**；四格之间**任意组合**（可两格数字两格字母、可四个都不同、可出现 `x` 与 `y` 同在题中等）。正负关系仍只通过式子里的 **`±` / 首项前的负号** 表达；**不把**「负数」直接写在一个**数字**本身上（字母项前仍用 `−x` 等表示负的未知数项）。
3. **书写（渲染）**：首项为正时，数字与字母按习惯省略不必要的正号（如正系数 `x` 不写 `+x` 于段首）；首项为负时写 `−…`。段内两项之间的 `+` / `−` 照常显示，便于和答案字符串对照。
4. **核心**（不变）：**双段**分别展开；段间为 **`−`** 时，须对**后一段括号内整体**变号（或等价规则）。首项为负或字母与符号混排时，易与括号外、段间符号**叠在一起**看错。

**答案形式（L3）**

1. **只展开，不计算**：与 L1 精神一致——答案侧**不要求**把含 `a、b、x、y` 的式子**代成数值**；**不要求**合并同类项（见全局约定）。只需完成**去括号后的展开式**，项的顺序按实现约定写死，保证四选一**唯一**。
2. **字母含义**：展开过程中 **`a、b` 始终作常数符号**，**`x、y` 作未知数符号**，不参与「算出最终数」的代入。

**错因模板（L3 专用）**

1. 段间为「−」时，第二段整体忘变号。
2. 段间为「−」时，第二段只变第一项符号，第二项未变号。
3. 段间为「−」时，第二段只变第二项符号，第一项未变号。
4. 段间为「− / +」时，第二段漏抄第一项。
5. 段间为「− / +」时，第二段漏抄第二项。
6. 段间为「+」时，第二段无故整体变号。
7. 段间为「+」时，第二段无故仅改变第一项符号。
8. 段间为「+」时，第二段无故仅改变第二项符号。
9. 某段首项前的「+ / −」被吃掉或重复变号。
10. 第一段与第二段符号混抄、段界不清。
11. 第一段漏抄第一项。
12. 第一段漏抄第二项。
13. 第一段展开后第一项符号错误。
14. 第一段展开后第二项符号错误。

---

## L4｜L1 / L3 混合 · 括号前系数 k（正整数）

**定位**：**运算骨架**混合 **L1**（一层括号、括号外加减）与 **L3**（两段括号并排、段间加减），并在**括号前**增加**正整数系数 `k`（或 `k1、k2`）**。**`A、B、C、D` 等项**的取值与 **L3** 相同：每格可为**整数**，或 **`a、b、x、y` 中任意一个**，同一题可**混用多个字母与数字**。**无括号嵌套**。**乘除去括号**见 **L2**；本档为**加减 + 系数×括号**的分配与双段变号。

**出题逻辑**

1. **两类外形**（实现可随机抽题型，占比自定）：
   - **L1 型 + 系数**：通式 **`± A ± k (± B ± C)`**。含义：由 **`±`** 连接的若干部分中，含**一项**与**一项「正整数 `k` × 一层括号」**；括号内为 **`± B ± C`**（两项用加减连接，规则同 L1）。整体最左可有符号；**首项前的加号可省略**（与 L3 一致）。
   - **L3 型 + 双系数**：通式 **`± k1 (± A ± B) ± k2 (± C ± D)`**。含义：与 **L3** 相同的两段括号并排、中间段间 **`±`**；**每一段括号前**各有一个**正整数系数** **`k1`、`k2`**（**`k1`** 乘左段括号，**`k2`** 乘右段括号）。**首项前的加号可省略**。

2. **系数**：**`k`、`k1`、`k2`** 均为**正整数**（建议 **1～12** 或与 L1 同档，由实现统一）。**不出现**系数为 0 或负数（负号只出现在**整体的 `±`** 或**括号内首项/项前**）。

3. **字母与数字**：**`A、B、C`**（及式子中其它与 **L3** 四项同级位置）规则同 **L3**：每格独立为**整数**或 **`a、b、x、y` 之一**；**`a、b`** 为常数项，**`x、y`** 为未知数；**可混用**（例如 **`A ± k(B ± C)`** 中 **`A、B、C`** 可为 **`x、3、y`** 等）。

4. **书写**：**系数紧贴括号**，乘号一般省略（如 **`2(x + 3)`**、**`k1(x + a) − k2(y − b)`**）；**最左首项**若为正，**正号省略**。

5. **核心**：
   - **分配律**：**`k(…)`** 须乘到括号内**每一项**（含 **`x`、`y`、`a`、`b`** 各项）；**`k1`、`k2`** 各自只乘**本段**括号内两项。
   - **双段型**：叠加 **L3** 的「段间 **`±`** → 后段整体变号」与 **每段 `ki×` 括号内** 的展开，避免只变号不乘 **`k`** 或只乘第一项。

**答案形式（L4）**

1. **只展开，不计算**：不代入数值；**不合并同类项**；展开式**项的顺序**由实现约定，保证四选一**唯一**。
2. **分配后**：每项须体现 **`k`（或 `k1`、`k2`）** 与括号内字母/数的正确乘积形式（**不**先把括号内算成一个数再乘）。

**错因模板（L4 专用）**

下述 **`k`** 可指本档中的 **`k`、`k1`、`k2`**。

1. 对于含 `± k (± B ± C)`，系数分配时将 `k` 前的 `±` 符号遗忘。
2. 对于含 `± k (± B ± C)`，`± k` 只乘括号内第一项。
3. 对于含 `± k (± B ± C)`，`± k` 只乘括号内第二项。
4. 对于含 `± k (± B ± C)`，分配时将第一项系数 `B` 或第二项系数 `C` 的符号弄错。
5. 对于含 `± k (± B ± C)`，系数 `± k` 直接被遗忘，得到结果为 `± B ± C`。
6. 对于含 `± k (± B ± C)`，分配时运算错误，例如 `± k (± B ± C) = ± k ± B ± C`，或 `± k (± B ± C) = ± k ± B ± k ± C`。
7. 对于 `± A ± k (± B ± C)`，误将 `A` 当成系数与括号内各项相乘。
8. 对于 `± A ± k (± B ± C)`，遗忘 `± A`。
9. 对于 `± A ± k (± B ± C)`，分配时误将 `k` 与 `± A` 相乘。
10. 双段中，段间或段首为「−」时，第二段整体忘变号。
11. 双段中，段间或段首为「−」时，第二段只变第一项符号，第二项未变号。
12. 双段中，段间或段首为「−」时，第二段只变第二项符号，第一项未变号。
13. 双段中，段间或段首为「− / +」时，第二段漏抄第一项。
14. 双段中，段间或段首为「− / +」时，第二段漏抄第二项。
15. 双段中，段间或段首为「+」时，第二段无故整体变号。
16. 双段中，段间或段首为「+」时，第二段无故仅改变第一项符号。
17. 双段中，段间或段首为「+」时，第二段无故仅改变第二项符号。
18. 某段首项前的「+ / −」被吃掉或重复变号。
19. 含括号的展开后分配时计算错误，如把 `k × (A ± B)` 错成 `(k × A) ± (k ± B)`。
20. 段间为「−」时，后一段的系数、括号内符号与段间负号三者交互处理错误。
21. 漏写含 **`x`、`y`** 的项，或漏写常数 **`a、b`** 项。

> **整合说明**：作者稿中 L4 原条目编号在「单段系数」与「双段」之间曾有跳号，本文已**连续重排**为 1～21，语义与《新拆括号等级说明.md》一致。

---

## L5｜单项×括号 · 除号型 · 两括号相乘（三种外形）

**定位**：在 **L4** 之后；**取值与 L4 相同**：**`A、B、C、D` 等**可为**整数**，或 **`a、b、x、y` 中任意字母**，同一题可**混用**。共**三种题型**，抽题比例 **1∶1∶2**（即 **25%**、**25%**、**50%**）。**无括号嵌套**（每层括号内仍为 **`±` 连接两项**）。**本档不考虑「一次式」限制**（见下「题型③」）。

**出题逻辑**

1. **题型①**（占 **1/4**）：**`± A × (± B ± C)`**  
   整体最左可有 **`±`**；**`A`** 与**一层括号**之间为**乘法**（书写可用 **`×`** 或按现网省略乘号）；括号内为 **`± B ± C`**。**首项前的加号可省略**。

2. **题型②**（占 **1/4**）：**`(± B ± C) ÷ A`**（用**除号 `÷`** 表示，**不是**分数形式）  
   只有**分子**一侧带一层括号 **`(± B ± C)`**；**除号后面不再写括号**——**`A`** 为**单独一项**（整数或 **`a、b、x、y` 之一**），**不出现** **`÷ ( … )`**。理由：除号后再跟括号则「拆括号」训练意义不大，本档不出。  
   **本模式一律不用**分数线、竖式分数；**只用**一行式里的 **`÷`**。

3. **题型③**（占 **1/2**）：**`(± A ± B) × (± C ± D)`**（两括号**交叉相乘**）  
   **两个**一层括号相乘；每段括号内均为 **`±` 连接两项**。本题型**专门训练乘法分配律**（逐项叉乘），**不按**全局「未知数一次式」限制（展开后可出现 **`xy`** 等项）。

4. **抽题比例**：三种题型 **1∶1∶2**（**题型③** 为 **50%**）。

5. **取值**：**`A、B、C、D`** 规则与 **L4** 相同：**每格独立**为**整数**或 **`a、b、x、y` 之一**；**`a、b`** 为常数项，**`x、y`** 为未知数。

6. **核心**：  
   - **题型①**：**`A`** 乘入括号 → **分配**到 **`B`、`C`**，注意符号。  
   - **题型②**：**一行除法**书写；分子括号去括号规则 + 与除数 **`A`** 的衔接（**无**「分母括号」）。  
   - **题型③**：两括号相乘展开为**四项**；**正确答案**在系统中的**输出顺序**约定为 **AC → AD → BC → BD**（见「答案形式」，仅为格式统一，**不**表示别的顺序算错）。

**答案形式（L5）**

1. **只展开，不计算**；**不合并同类项**；不代入数值。
2. **题型③（两括号相乘）**：设第一括号为 **`(± A ± B)`**、第二括号为 **`(± C ± D)`**（**`A、B、C、D`** 为四项「位置」，各可为整数或字母）。展开后的**四个乘积项**，**标准答案（正确答案）**按固定顺序输出：**AC**、**AD**、**BC**、**BD**（即先 **`A`** 乘 **`C`、`D`**，再 **`B`** 乘 **`C`、`D`**；每项内部符号由展开规则决定，**不合并**）。  
   **说明**：该顺序**仅用于统一正确答案字符串/判对格式**；**代数上与正确答案等价**但**项的书写顺序不同**的展开，**不算数学错误**。**不要**用「四项顺序错了」来构造干扰项——顺序问题**不是**本模式要考的错因。
3. **本档**（尤其题型③）**不适用**「未知数必须一次式」的限制；以**分配律训练**为准。

**错因模板（L5 专用）**

1. 对于 `± A × (± B ± C)`，`± A` 分配时将 `A` 前的 `±` 符号遗忘。
2. 对于 `± A × (± B ± C)`，`± A` 只乘括号内第一项。
3. 对于 `± A × (± B ± C)`，`± A` 只乘括号内第二项。
4. 对于 `± A × (± B ± C)`，分配时将第一项系数 `B` 前或第二项系数 `C` 前的符号弄错。
5. 对于 `± A × (± B ± C)`，遗忘 `± A`，直接显示结果为 `± B ± C`。
6. 对于 `± A × (± B ± C)`，分配时运算错误，例如 `± A × (± B ± C) = ± A ± B ± C`，或 `± A × (± B ± C) = ± A ± B ± k ± C`（式中多余常数仅为示例性错形）。
7. 对于 `(± B ± C) ÷ A`，只除括号内第一项，第二项未除。
8. 对于 `(± B ± C) ÷ A`，只除括号内第二项，第一项未除。
9. 对于 `(± B ± C) ÷ A`，除法分配时将括号内第一项 `B` 前符号与第二项 `C` 前符号两者弄混。
10. 对于 `(± B ± C) ÷ A`，除法分配时将括号内第一项 `B` 前符号或第二项 `C` 前符号看错。
11. 对于 `(± A ± B) × (± C ± D)`，乘法分配时漏乘第一个或第二个括号中某项。
12. 对于 `(± A ± B) × (± C ± D)`，乘法分配时漏乘某一括号中某项。
13. 对于 `(± A ± B) × (± C ± D)`，乘法分配时改变第一个括号内某项符号（即 `A` 与 `B` 前的符号在分配时某一个被改变）。
14. 对于 `(± A ± B) × (± C ± D)`，乘法分配时改变第一个括号内整体符号（即 `A` 与 `B` 前的符号在分配时均被改变）。
15. 对于 `(± A ± B) × (± C ± D)`，乘法分配时改变第二个括号内某项符号（即 `C` 与 `D` 前的符号在分配时某一个被改变）。
16. 对于 `(± A ± B) × (± C ± D)`，乘法分配时改变第二个括号内整体符号（即 `C` 与 `D` 前的符号在分配时均被改变）。
17. 对于 `(± A ± B) × (± C ± D)`，漏掉交叉项（只做了 `AC` 和 `BD`）。
18. 对于 `(± A ± B) × (± C ± D)`，漏掉 `AC` / `AD` / `BC` / `BD` 中某一项（**一项**，非多项）。
19. 对于 `(± A ± B) × (± C ± D)`，弄错分配后 `AC` / `AD` / `BC` / `BD` 中某一项前的系数（**一项**，非多项）。

> **整合说明**：作者稿 L5 第 12 条原文「第个括号」已按语义改为「某一括号」；原稿中空条目未纳入。

---

## 其它（产品 / 实现）

1. **升降级与局内规则**：仍以现网拆括号模式为准（如 20 题、即时讲解、Next、全对升 / 错多降等），除非产品另改。
2. **与管理端**：`admin.html` 等与关卡时间、模式名相关的配置，随关卡描述变更时一并核对。
3. **实现要点**：**生成器**按档位抽结构；**标准答案**用同一套「线性项 + 常数项」的**不合并**序列渲染成字符串；**干扰项**从**当前档适用的错因表**中选取（多于 4 条时随机 4 条），每条对应一类错误答案，且去重。
4. **本文未涉细节**：各档具体随机占比、是否禁止某子结构，可在定稿后补一行「实现备注」。
5. **L5 题型③**：干扰项**不要**使用「与正确答案仅**项序不同**、代数仍等价」的选项（顺序仅用于标准答案格式统一）。

---

## 修订记录

| 日期 | 说明 |
|------|------|
| 2026-04-02 | 助手整理：补全局约定、L1–L3 排版与 L3「首项前 ±」表述、补全 L4/L5 草案、其它与实现提示。 |
| 2026-04-02 | L1–L5 错因模板改为四列（1～4）对照表；L2/L4 第四列为「—」。 |
| 2026-04-02 | 取消横排列表：新增「错因模板全库」19 条一行一条；各级别改为逐条列出并引用全库编号。 |
| 2026-04-02 | 全局约定、L1–L5 出题说明、「其它」改为与错因相同的逐条列表；各级增加小标题「出题逻辑」。 |
| 2026-04-02 | L3 出题逻辑改为实现向表述：段间/段内 `±` 含义、数字为绝对值、书写省略正号、核心不变。 |
| 2026-04-02 | **L2 重写**：乘除去括号；括号与 `k` 仅乘除；括号内两数、`op` 可为 `+−×÷`；括号可在左或右。全库增 20–24。L4/L5 文案去掉对已废弃「L2 三项」的引用。 |
| 2026-04-02 | **L2 答案形式**：只拆括号不计算；分配/去括号变形；`( )÷k` 可除法分配拆开；`k÷( )` 正确照抄题面，错项用假分配。全库增 25。 |
| 2026-04-02 | L2：恢复「变号」用语并释义（×/÷ 运算符号规则，非泛称变形）；澄清 `(A×B)÷k` 仅一种正确展开，误拆为错解非二选一。 |
| 2026-04-02 | **L3**：运算规则不变；打破纯整数，`A～D` 可取整数或 **`a、b、x、y`**（ab 常数、xy 未知数）；只展开不计算。全局约定字母/展示同步调整。 |
| 2026-04-02 | **L3** 澄清：同一题可**混用多个字母与数字**（例 `(x+3)−(y−b)`），非整题只选一个字母。 |
| 2026-04-02 | **L4 重写**：混合 L1 型 **`±A±k(±B±C)`** 与 L3 型 **`±k1(±A±B)±k2(±C±D)`**；**`k、k1、k2`** 为正整数；**`A～D`** 同 L3（整数或 **`a、b、x、y`**）；首项正号可省略。全局约定 L4 与 L3 共用字母规则。 |
| 2026-04-02 | **L5 重写**：三种题型 **`±A×(±B±C)`**、**`(±B±C)/(±A)`**、**`(±A±B)×(±C±D)`**，比例 **1∶1∶2**；取值同 L4。全局：L5 纳入 **abxy**；**次数**补充第三型可能与 **`xy`** 项的关系。 |
| 2026-04-02 | **L5**：题型②改为 **`(±B±C)÷A`**（除号、非分数）；除号后**无括号**；题型③ 答案顺序 **AC、AD、BC、BD**；L5 **不考虑一次式**。全局去掉 L5 分数展示、次数与 **L5** 对齐。 |
| 2026-05-12 | **错因改版**：废除「错因模板全库」维护方式；并入《新拆括号等级说明.md》各档独立错因表；补充错因选取策略（适用集 >4 则随机 4）；L4 错因编号在本文内连续化；L5 修正作者稿笔误并删空条。 |
