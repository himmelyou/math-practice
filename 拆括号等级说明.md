# 拆括号模式 · 出题规格（题型 + 错因）

> **读者**：产品/教研定稿；实现见 `docs/shared/expand-brackets-questions.js`（与 `docs/index.html` 内联块需保持同源）。  
> **本文即唯一规格**：改逻辑先改本文，再改代码（或双向 diff）。

---

## 1. 文档怎么用

本文分两部分：

| 部分 | 内容 | 读者 |
|------|------|------|
| **正文**（§3 起各等级） | 题型、占比、出题参数、正确答案、错因 | 你（逻辑定稿） |
| **附录 · 程序实现**（文末） | `expand-brackets-questions.js`、`index.html` 选项组装等 | 开发 / AI |

正文每一**等级**只写：

1. **题型一览**（ID、题面模式、占比）
2. **逐题型**：参数 → 正确答案 → **错因表**

**逻辑上的出题顺序**（不涉及代码字段）：

```text
抽等级 → 按占比抽题型 → 在题型内抽参数（数字、括号内加减等）
       → 写出正确答案 → 从本题型的错因里取 3 条作干扰项（见 §2）
```

程序细节（函数名、数据结构、渲染）一律见**附录**。

---

## 2. 全局约定（各档默认；档内另有说明者以档内为准）

| 项目 | 规则 |
|------|------|
| 答案 | 只展开一层；不合并同类项；不把字母代入成数；不把整式算成单个得数 |
| 数字 | 未说明时：正整数，范围见各档题型参数 |
| 字母 | L1、L2：无字母。L3、L4：仅 **`x`**。L5：细则待重写（原稿含 `a,b,x,y`） |
| 书写 | 数×数保留 `×`；数×字母省略乘号；同字母乘积写幂；L5 除法用 `÷` 不用分数线 |
| 选项 | 每题 **4 个选项 = 1 正确 + 3 错误** |
| 干扰项 | 只从**当前题型**的错因表取；每题需要 **3 条**错因；若本题型错因 **>3 条**，则**随机抽 3 条**；每条对应一类固定错误式，互不重复 |
| 错因编号 | **按题型单独编号**（每个题型从 **1** 起，与别的题型无关）；编号只在该题型内有效 |

---

## 3. 等级索引

| 等级 | 名称 | 题型数 | 文档状态 |
|------|------|--------|----------|
| L1 | 一层括号 · 整数加减 | 2 | **已定稿**（题型 + 错因） |
| L2 | 乘除去括号 | 6 | **已定稿**（题型 + 错因） |
| L3 | 简单代数式 · 分配并算积 | 4 | **已定稿**（题型 + 错因 + 代码） |
| L4 | 代数式进阶 · 分配并算积（可负号、可 x²） | 4 | **已定稿**（题型 + 错因；代码待写） |
| L5 | 单项×括号 / 除号 / 两括号相乘 | 3 | 待重写 |

---

## L1｜一层括号 · 整数

**档内约束**：仅整数；一层括号；括号内恰两项（`B`、`C`）；括号外为 `+` 或 `−`。

### 题型一览

| 题型 ID | 题面模式 | 占比 | 训练点 |
|---------|----------|------|--------|
| L1-T1 | `A − (B ± C)` | **80%** | 括号外为减：去括号后括号内**逐项变号** |
| L1-T2 | `A + (B ± C)` | **20%** | 括号外为加：去括号后**符号不变** |

### 题型内公共参数（两型共用）

| 参数 | 规则 |
|------|------|
| `A`、`B`、`C` | 均为 **1～12** 的正整数 |
| 括号内是「加」还是「减」 | 各约 **50%** |
| 正确答案 | 去括号后的**三项**（用加减连接）；不合并同类项；不算成一个总数 |

**正确答案（展开后的三项）**

| 题型 | 括号内是 | 三项依次是 |
|------|----------|------------|
| L1-T1 | 加 | `A`，`−B`，`−C` |
| L1-T1 | 减 | `A`，`−B`，`+C` |
| L1-T2 | 加 | `A`，`+B`，`+C` |
| L1-T2 | 减 | `A`，`+B`，`−C` |

示例：题面 `7 − (3 + 2)` → 正确答案 `7 − 3 − 2`。

---

### L1-T1｜`A − (B ± C)` · 占比 80%

本题型错因 **3** 条，每题 **3** 条全用（无需再抽）。

| 错因 | 错因简述 | 错误答案长什么样 |
|------|----------|------------------|
| 1 | 括号外是减号，但括号里两项都**没变号** | 去掉括号后，两项符号与括号内原来一样 |
| 2 | 括号外是减号，但**只变了第一项**的符号 | 只第一项变号，第二项不变 |
| 3 | 括号外是减号，但**只变了第二项**的符号 | 第一项不变，只第二项变号 |

---

### L1-T2｜`A + (B ± C)` · 占比 20%

本题型错因 **3** 条，每题 **3** 条全用。

| 错因 | 错因简述 | 错误答案长什么样 |
|------|----------|------------------|
| 1 | 括号外是加号，却把括号里**两项都变号**了 | 不该变号却两项都变 |
| 2 | 括号外是加号，却**只改了第一项**的符号 | 只第一项被无故改掉符号 |
| 3 | 括号外是加号，却**只改了第二项**的符号 | 只第二项被无故改掉符号 |

---

> **以下 L5 为逻辑草稿**（待改写成「题型 + 错因」正文）。L3、L4 已定稿；现网行为见文末**附录 · 程序实现**。

---

## L2｜乘除去括号

**档内约束**

- 仅**正整数**；**无字母**。
- **一层括号**；括号内恰两项 `A`、`B`。
- 括号外只有一个数 **`k`**；`k` 与括号之间只能是 **`×` 或 `÷`**（不能是 `+`、`−`）。
- `k` 可在括号**左边**或**右边**（题面随机其一）。
- **只拆括号，不计算**：答案写**展开后的式子**；不把括号内外先算成一个数；不合并同类项。
- 括号内连接记为 **`op`**：本档 **`op` 只能是 `+`、`−`、`×`、`÷` 之一**（由题型决定，见下表）。

---

### 题型一览

| 题型 ID | 题面模式（`k` 在左/右随机） | 括号内 `op` | 占比 | 训练点 |
|---------|---------------------------|-------------|------|--------|
| L2-T1 | `k × (A op B)` 或 `(A op B) × k` | 仅 **`+` / `−`** | **20%** | 乘法分配律 |
| L2-T2 | `k × (A op B)` 或 `(A op B) × k` | 仅 **`×` / `÷`** | **10%** | 内层乘除：去括号写**连乘除**，勿误用加减分配 |
| L2-T3 | `k ÷ (A op B)` | 仅 **`+` / `−`** | **20%** | **不能去括号**（见该题型正确答案） |
| L2-T4 | `k ÷ (A op B)` | 仅 **`×` / `÷`** | **20%** | 可去括号；内层 **× / ÷ 按规则变号**（见 T4，非 L1 正负变号） |
| L2-T5 | `(A op B) ÷ k` | 仅 **`+` / `−`** | **20%** | 除法对加减的分配：`A÷k ± B÷k` |
| L2-T6 | `(A op B) ÷ k` | 仅 **`×` / `÷`** | **10%** | 去括号为**唯一**连乘除式；勿把内层乘除当加减去拆 |

> 六题型占比合计 **100%**（20% + 10% + 20% + 20% + 20% + 10%）。现网 `layout` 权重与上表**不完全一致**，见附录 L2。

### 题型内公共参数（六型共用）

| 参数 | 规则 |
|------|------|
| `k`、`A`、`B` | 均为 **1～12** 的正整数 |
| 括号内加/减 | 各约 **50%**（仅 T1、T3、T5） |
| 括号内乘/除 | 各约 **50%**（仅 T2、T4、T6） |
| `k` 在左还是右 | 各约 **50%**（左右两种题面随机） |

---

### L2-T1｜外层 `×`，内层 `±` · 占比 20%

**题面**：`k × (A + B)`、`k × (A − B)`、`(A + B) × k`、`(A − B) × k`（四种之一随机）。

**正确答案**：用乘法分配律展开，例如 `k × (A + B)` → `k × A + k × B`；`k × (A − B)` → `k × A − k × B`（`(A ± B) × k` 同理，**不算** `k×A` 的数值）。

**错因**（共 **4** 条，每题**随机抽 3** 条）

| 错因 | 错因简述 | 错误答案长什么样 |
|------|----------|------------------|
| 1 | 分配时**漏乘第一项** | 只剩 `k × B`（或等价漏项） |
| 2 | 分配时**漏乘第二项** | 只剩 `k × A`（或等价漏项） |
| 3 | 分配时**第二项符号看错** | 括号内是加却写成减（或反之） |
| 4 | 把括号内**加减看成乘除** | 写成 `k × (A × B)` 一类 |

---

### L2-T2｜外层 `×`，内层 `×` / `÷` · 占比 10%

**题面**：`k × (A × B)`、`k × (A ÷ B)`、`(A × B) × k`、`(A ÷ B) × k`（四种之一随机）。

**正确答案**：去掉括号，写成一条**连乘除**式（**不**拆成 `k×A ± k×B`），例如：

- `k × (A × B)` → `k × A × B`
- `k × (A ÷ B)` → `k × A ÷ B`
- `(A × B) × k` → `A × B × k`
- `(A ÷ B) × k` → `A ÷ B × k`

**错因**（**3** 条，每题全用）

| 错因 | 错因简述 | 错误答案长什么样 |
|------|----------|------------------|
| 1 | 误用乘法分配，对两项都乘 `k` 却**没去掉括号** | 如 `k × A × B` 写成 `k × A op k × B` 且 `op` 仍像括号内原符号 |
| 2 | 把内层 **× / ÷ 看成 + / −** 去分配 | 如 `k × A + k × B` |
| 3 | 把内层看成 **±** 且**只去了一半括号** | 如 `k × A + B` 或 `A + B × k` |

---

### L2-T3｜`k ÷ (A ± B)` · 占比 20%

**题面**：`k ÷ (A + B)` 或 `k ÷ (A − B)`。

**正确答案**：**不能去括号**——答案与题面**相同**（或选项文案：**「此类情况无法去除括号」**）。训练点：除号后是「和/差」时，**不要**强行拆成 `k÷A ± k÷B`。

**错因**（**3** 条，每题全用）

| 错因 | 错因简述 | 错误答案长什么样 |
|------|----------|------------------|
| 1 | **强行分配**两项 | `k ÷ A + k ÷ B`（符号也可能错） |
| 2 | **只「分配」了一项** | 如只有 `k ÷ A`，或只 `k ÷ B` |
| 3 | 把括号内 **± 看成 ×** | 写成 `k ÷ (A × B)` |

---

### L2-T4｜`k ÷ (A × B)` 或 `k ÷ (A ÷ B)` · 占比 20%

**题面**：`k ÷ (A × B)` 或 `k ÷ (A ÷ B)`（内层乘、除各约一半）。

**正确答案**：**可以去掉括号**，写成一条连乘除式；本档说的**「变号」**指内层 **× / ÷ 符号怎么改**（不是 L1 那种每一项正负变号）：

| 题面 | 去括号后（规范写法） | 说明 |
|------|----------------------|------|
| `k ÷ (A × B)` | `k ÷ A ÷ B` | 内层是 **×**，去括号后接成**连除** |
| `k ÷ (A ÷ B)` | `k ÷ A × B` | 内层是 **÷**，去括号后 **÷ 变 ×** |

**不算**最终数值；**不要**写成 `k ÷ A + k ÷ B`，也**不要**写成 `k ÷ A × k ÷ B`（把内层乘除误当加减、或误用「分别除」分配）。

**错因**（**3** 条，每题全用）

| 错因 | 错因简述 | 错误答案长什么样 |
|------|----------|------------------|
| 1 | 误用「分别除」式分配 | `k ÷ A × k ÷ B` |
| 2 | 内层是 `A ÷ B` 时，**÷ 没变 ×** | 写成 `k ÷ A ÷ B`（应多一个 × 衔接） |
| 3 | 内层是 `A × B` 时**项顺序写反** | 如 `k ÷ B ÷ A`（与 `k ÷ A ÷ B` 规范写法不符） |

---

### L2-T5｜`(A ± B) ÷ k` · 占比 20%

**题面**：`(A + B) ÷ k` 或 `(A − B) ÷ k`。

**正确答案**：除法分配展开，例如 `(A + B) ÷ k` → `A ÷ k + B ÷ k`；`(A − B) ÷ k` → `A ÷ k − B ÷ k`（**不算商**）。

**错因**（共 **5** 条，每题**随机抽 3** 条）

| 错因 | 错因简述 | 错误答案长什么样 |
|------|----------|------------------|
| 1 | 把括号内 **± 看成 ×** | 写成 `(A × B) ÷ k` |
| 2 | 括号内是 **+ 却按 −** 去分配 | 第二项符号错 |
| 3 | **第一项漏 ÷ k** | 如 `A + B ÷ k` |
| 4 | **第二项漏 ÷ k** | 如 `A ÷ k + B` |
| 5 | 把括号外 **÷ 当成 ×** | 按 `(A ± B) × k` 去乘开 |

---

### L2-T6｜`(A × B) ÷ k` 或 `(A ÷ B) ÷ k` · 占比 10%

**题面**：`(A × B) ÷ k` 或 `(A ÷ B) ÷ k`。

**正确答案**：去括号为**唯一**连乘除式，例如 `(A × B) ÷ k` → `A × B ÷ k`；`(A ÷ B) ÷ k` → `A ÷ B ÷ k`。**不要**写成 `A÷k × B÷k`（那是把内层乘除误当加减分配）。

**错因**（共 **4** 条，每题**随机抽 3** 条）

| 错因 | 错因简述 | 错误答案长什么样 |
|------|----------|------------------|
| 1 | 对 `(A × B) ÷ k` **误用加减分配** | `A ÷ k × B ÷ k` 或 `A ÷ k + B ÷ k` |
| 2 | `(A × B) ÷ k` **拆写混乱** | 如 `A × k ÷ B` |
| 3 | 把内层 **× / ÷ 当成 ±** 去分配 | 如 `A ÷ k + B ÷ k` |
| 4 | **乘除号顺序**写反 | 如该 `A × B ÷ k` 却写成 `B × A ÷ k` 等 |

---

## L3｜简单代数式 · 分配并算积

**档内约束**

- **仅字母 `x`**（一次项）；本档不出现 `a、b、y` 等其他字母。
- **不引入负数概念**：数与 `x` 的系数均为 **1～12 的正整数**；式子中只允许用 **`+` / `−` 连接**各项，不出现「−5」「−3x」这种带负号的前缀数或项（减法通过「减后面一整段」表达，见各题型）。
- **题面骨架**：`A` 与 **`k(B ± C)`** 用 **`+` 或 `−`** 连接；**`k` 为 2～12 的正整数**（**不得为 1**，否则「忘乘 `k`」类错因无法与正确答案区分）。
- **括号内**：`B`、`C` **一项为常数、一项为 `□x`**（系数 1～12）；**常数在前 / `x` 项在前** 各约 **50%**。
- **`A` 的两种类型**（各约 **50%**）：**常数**（如 `5`），或 **一次项**（如 `8x`；系数 1 写作 `x`）。
- **布局**：约 **80%** 为 **`A ± k(B ± C)`**（`A` 在左）；约 **20%** 为 **`k(B ± C) ± A`**（左右对调，括号块在左）。
- **答案**：展开式；**`k` 与括号内常数、`x` 系数相乘并写出积**（如 `2(3+4x)` → `6+8x`）；**不合并同类项**；题面、正确答案、四个选项及错因描述中**均不出现乘号 `×`**（不写 `2×3+8x` 这类留痕式）。

---

### 题型一览

| 题型 ID | 两段之间的连接 | 括号内 | 占比 | 训练点 |
|---------|----------------|--------|------|--------|
| L3-T1 | **`+`** | **`+`** | **25%** | 外「加」：分配后两项与 `A` 直接相加；算积 |
| L3-T2 | **`+`** | **`−`** | **25%** | 外「加」、内「减」：分配后第二项符号 |
| L3-T3 | **`−`** | **`+`** | **25%** | 外「减」：减去括号 → 分配后两项变号；算积 |
| L3-T4 | **`−`** | **`−`** | **25%** | 外「减」、内「减」：符号叠在一起最易错 |

> 另有 **布局 20% 对调**、**`A` 常数/一次项各 50%**、**括号内项序各 50%**，见下「公共参数」；与上表四维独立随机（实现时按占比抽题型即可）。

### 题型内公共参数

| 参数 | 规则 |
|------|------|
| `k` | **2～12** 的正整数 |
| 常数、一次项系数 | **1～12** 的正整数 |
| `A` 是常数还是 `□x` | 各 **50%** |
| 题面布局 | **80%** `A ± k(B ± C)`；**20%** `k(B ± C) ± A` |
| 括号内：常数在前还是 `x` 在前 | 各 **50%** |
| 括号内 `+` / `−` | 由题型 T1～T4 决定 |

### 正确答案（通则）

1. **先分配**：`k` 乘括号内**每一项**，并把 **常数积、`x` 系数积算到底**（正整数结果）。
2. **再处理外连接符**：外为 **`+`** 时，展开项与 `A` **同号相加**；外为 **`−`** 时，展开后的两项**分别变号**再与 `A` 用 `+`/`−` 衔接（与 L1 减括号精神一致）。
3. **不合并**：例如 `8x + 15 − 6x` **保持三项**，不收成 `2x + 15`。
4. **项的书写顺序**（保证四选一唯一）：
   - **布局 80%**（`A` 在左）：**先写 `A` 项**，再写括号内**从左到右**分配后的两项；
   - **布局 20%**（对调）：**先写**括号内从左到右分配后的两项，**再写 `A` 项**。
5. **书写**：各项之间用 `+` / `−`；`x` 项系数为 1 时写 `x`；正号按现网习惯省略。

**示例（布局 80%，`A` 为常数）**

| 题型 | 题面 | 正确答案 |
|------|------|----------|
| L3-T1 | `5 + 2(3 + 4x)` | `5 + 6 + 8x` |
| L3-T2 | `5 + 3(5 − 2x)` | `5 + 15 − 6x` |
| L3-T3 | `5 − 2(3 + x)` | `5 − 6 − 2x` |
| L3-T4 | `5 − 2(4 − x)` | `5 − 8 + 2x` |

**示例（布局 20% 对调）**

| 题型 | 题面 | 正确答案 |
|------|------|----------|
| L3-T1 | `2(3 + 4x) + 5` | `6 + 8x + 5` |
| L3-T3 | `2(3 + x) − 5` | `6 + 2x − 5` |

（对调时外连接符仍是「加」或「减」；T2/T4 同理，先括号展开两项再写 `A`。）

---

### L3-T1｜外 `+` · 内 `+` · 占比 25%

**错因**（共 **4** 条，每题**随机抽 3** 条）

| 错因 | 错因简述 | 错误答案长什么样 |
|------|----------|------------------|
| 1 | **第二项**忘了乘 `k`（最常见） | 如 `5+2(3+4x)` 写成 `5+6+4x`（`6` 对了，`4x` 未乘 `2`） |
| 2 | **两项**都忘了乘 `k` | 如写成 `5+3+4x`（括号内两项原样抄出，均未乘 `k`） |
| 3 | 内层「加」看成「减」（或反之） | 第二项符号错 |
| 4 | **`k` 与系数相乘算错** | 两项都乘了 `k`，但积算错（如 `8x` 写成 `6x`） |

---

### L3-T2｜外 `+` · 内 `−` · 占比 25%

**错因**（**4** 条，随机抽 **3**）

| 错因 | 错因简述 | 错误答案长什么样 |
|------|----------|------------------|
| 1 | **第二项**忘了乘 `k` | 如 `5+3(5−2x)` 写成 `5+15−2x`（`15` 对了，`2x` 未乘 `3`） |
| 2 | **两项**都忘了乘 `k` | 如写成 `5+5−2x`（括号内两项原样抄出，均未乘 `k`） |
| 3 | 内层应是「减」却按「加」分配 | 第二项符号错 |
| 4 | 乘积算错 | 两项都乘了 `k`，但常数积或 `x` 系数积错误 |

---

### L3-T3｜外 `−` · 内 `+` · 占比 25%

**错因**（**4** 条，随机抽 **3**）

| 错因 | 错因简述 | 错误答案长什么样 |
|------|----------|------------------|
| 1 | 外「减」却未变号：括号两项符号都不变 | 如 `5−2(3+x)` 写成 `5+6+2x` |
| 2 | 只变第一项 / 只变第二项 | 变号规则只做对一半 |
| 3 | **第二项**变号对但忘了乘 `k` | 如写成 `5−6−x`（应是 `5−6−2x`） |
| 4 | 变号对但**两项**都忘了乘 `k`，或乘积算错 | 如写成 `5−3−x`（均未乘 `k`），或变号、算积有一处错 |

---

### L3-T4｜外 `−` · 内 `−` · 占比 25%

**错因**（**4** 条，随机抽 **3**）

| 错因 | 错因简述 | 错误答案长什么样 |
|------|----------|------------------|
| 1 | 外「减」+ 内「减」：变号规则全错 | 两项符号都与正确展开相反 |
| 2 | 只变一项、或内层减号看错 | 只变号一项或内层符号错 |
| 3 | **第二项**符号处理对但忘了乘 `k` | 如 `5−2(4−x)` 写成 `5−8+x`（应是 `5−8+2x`） |
| 4 | 变号、乘 `k` 都试了但**算积错** | 数字或 `x` 的系数错误 |

---

> **与旧版 L3 关系**：原「两括号并排 · abxy」逻辑**废弃**；`expand-brackets-questions.js` 已按本节实现。L4/L5 梯度另议。

---

## L4｜代数式进阶 · 分配并算积（可负号、可 x²）

**档内约束**

- **仅字母 `x`**（与 L3 相同）；可出现 **`x²`**（当 **`B` 为一次项**且括号内也含 **`x`** 时，分配后须**算积到底**，如 `2x · 4x → 8x²`）。
- **题面骨架**：**`±A ± B(±C ± D)`**（一层括号；**无** L3 旧稿「两括号并排」外形）。**`A`、`B`、`C`、`D`** 各自可为**常数**或 **一次项**（系数绝对值 **1～12**）；括号内 **`C`、`D` 一项为常数、一项为 `□x`**（同 L3 的 `B/C` 角色）。
- **负号规则**（与 L3 不同）：
  - **允许**在**前面没有加减号**的位置写**首项负号**：**`A`**（整式最左或布局对调后的首段）、括号内**第一项 `C`**（如 `-5 + 2x(…)`、`-3 + 4x`、`2x(-3 + 5)`）。
  - **`B` 本身不写负号**：`B` 前**必有**段间 **`+` / `−`**（如 `+ 2x(…)`、`− 3(…)`），负号体现在**减整段** `B(…)`，而非 `-2x(…)` 这种「乘数带负号」。
  - 括号内**第二项 `D`** 仍用 **`+` / `−` 连接**（如 `3 − 4x`），**不**写成 `3 + (-4x)` 式前缀负号。
- **`B`（乘在括号前）**：
  - **常数**：绝对值 **2～12**（**不得为 1**，否则「忘乘 `B`」错因不可辨）。
  - **一次项**：系数绝对值 **2～12**（**不得为 `x`**，即系数不得为 1）。
  - **`B` 的数值部分恒为正**；加减由 **`±B(…)`** 前的连接符表达。
- **布局、四题型、算积/不合并/无乘号选项**：与 **L3** 相同（见下表；把 L3 的 **`k` 换成 `B`**，**`A` 仍指括号外独立项**，括号内为 **`C`、`D`**）。

---

### 题型一览

| 题型 ID | `A` 与 `B(…)` 之间 | 括号内 `C` 与 `D` 之间 | 占比 | 训练点 |
|---------|---------------------|------------------------|------|--------|
| L4-T1 | **`+`** | **`+`** | **25%** | 外「加」：分配后两项与 `A` 直接相加；算积（含可能的 `x²`） |
| L4-T2 | **`+`** | **`−`** | **25%** | 外「加」、内「减」 |
| L4-T3 | **`−`** | **`+`** | **25%** | 外「减」：减去 `B(…)` → 分配后两项变号（规则同 L3） |
| L4-T4 | **`−`** | **`−`** | **25%** | 外「减」、内「减」 |

> **布局 20% 对调**、**`A` / `B` 常数或一次项各 50%**、**括号内项序各 50%**、**`A` / `C` 是否带首项负号** 的占比由实现约定（建议：首项负号各约 **30%～50%**，且不与「仅允许首项负号」冲突）；与上表独立随机。

### 题型内公共参数

| 参数 | 规则 |
|------|------|
| `A` | 常数或 `□x`；可为 **`-□` / `-□x`**（首项负号） |
| `B` | 常数 **2～12** 或 **一次项**（系数 **2～12**）；**不写** `-B` 前缀 |
| `C`、`D` | 一项常数、一项 `□x`；**`C` 可**为 **`-□` / `-□x`**；**`D` 用 `±` 连接** |
| 题面布局 | **80%** `A ± B(C ± D)`；**20%** `B(C ± D) ± A` |
| 括号内：常数在前还是 `x` 在前 | 各 **50%** |
| 内外 `+` / `−` | 由 **L4-T1～T4** 决定 |

### 正确答案（通则）

1. **先分配**：**`B` 乘括号内每一项**，**常数积、一次项积、`x·x` 积算到底**（可出现 **`x²`**）；题面/选项/错因描述中**不写乘号 `×`**（不写 `2x·4x` 留痕式）。
2. **再处理外连接符**：与 **L3** 相同——**仅**在布局 **80%** 且 **`A − B(…)`** 时，对分配后的两项**变号**；**`B(…) − A`**（布局 20%）时分配项**不变号**、只在末尾减 **`A`**。
3. **不合并同类项**（如 `6x` 与 `8x²` 并存）。
4. **项序**：与 L3 相同——**80%** 先 `A` 再括号内从左到右两项；**20%** 先两项再 `A`。
5. **书写**：`x` 系数为 1 写作 `x`（**`B` 不为 1** 故括号前一次项不出现裸 `x`）；`x²` 按全局约定写幂；首项负号按题面保留到展开式（如 `-5 + 6x + 8x²`）。

**示例（布局 80%）**

| 题型 | 题面 | 正确答案 |
|------|------|----------|
| L4-T1 | `-5 + 2x(3 + 4x)` | `-5 + 6x + 8x²` |
| L4-T2 | `7 + 3(5 − 2x)` | `7 + 15 − 6x` |
| L4-T3 | `5 − 2x(3 + x)` | `5 − 6x − 2x²` |
| L4-T4 | `4 − 3(4 − x)` | `4 − 12 + 3x` |

**示例（`C` 首项负号）**

| 题面 | 正确答案 |
|------|----------|
| `6 + 2x(-3 + 5)` | `6 − 6x + 10x` |
| `10 − 4(-2 + 3x)` | `10 + 8 − 12x` |

**示例（布局 20% 对调）**

| 题型 | 题面 | 正确答案 |
|------|------|----------|
| L4-T1 | `2x(3 + 4x) + 5` | `6x + 8x² + 5` |
| L4-T3 | `2x(3 + x) − 5` | `6x + 2x² − 5` |

---

### L4-T1｜外 `+` · 内 `+` · 占比 25%

**错因**（共 **4** 条，每题**随机抽 3** 条；语义同 **L3-T1**，将 **`k` 改为 `B`**）

| 错因 | 错因简述 | 错误答案长什么样 |
|------|----------|------------------|
| 1 | **第二项**忘了乘 `B`（最常见） | 如 `5+2x(3+4x)` 写成 `5+6x+4x` |
| 2 | **两项**都忘了乘 `B` | 括号内两项原样抄出，均未乘 `B` |
| 3 | 内层「加」看成「减」（或反之） | 第二项符号错 |
| 4 | **乘积算错** | 两项都乘了 `B`，但常数积、`x` / `x²` 系数积有错 |

---

### L4-T2｜外 `+` · 内 `−` · 占比 25%

**错因**（**4** 条，随机抽 **3**；同 **L3-T2**，`k`→`B`）

| 错因 | 错因简述 | 错误答案长什么样 |
|------|----------|------------------|
| 1 | **第二项**忘了乘 `B` | 第一项积对了，第二项未乘 `B` |
| 2 | **两项**都忘了乘 `B` | 括号内两项原样抄出 |
| 3 | 内层应是「减」却按「加」分配 | 第二项符号错 |
| 4 | 乘积算错 | 常数积或 `x` / `x²` 项有错 |

---

### L4-T3｜外 `−` · 内 `+` · 占比 25%

**错因**（**4** 条，随机抽 **3**；同 **L3-T3**，`k`→`B`）

| 错因 | 错因简述 | 错误答案长什么样 |
|------|----------|------------------|
| 1 | 外「减」却未变号：括号两项符号都不变 | 如 `5−2x(3+x)` 写成 `5+6x+2x²` |
| 2 | 只变第一项 / 只变第二项 | 变号只做对一半 |
| 3 | **第二项**变号对但忘了乘 `B` | 如少乘一次项或漏 `x²` |
| 4 | 变号对但**两项**都忘了乘 `B`，或乘积算错 | 均未乘 `B`，或变号、算积有一处错 |

---

### L4-T4｜外 `−` · 内 `−` · 占比 25%

**错因**（**4** 条，随机抽 **3**；同 **L3-T4**，`k`→`B`）

| 错因 | 错因简述 | 错误答案长什么样 |
|------|----------|------------------|
| 1 | 外「减」+ 内「减」：变号规则全错 | 两项符号都与正确展开相反 |
| 2 | 只变一项、或内层减号看错 | 只变号一项或内层符号错 |
| 3 | **第二项**符号处理对但忘了乘 `B` | 如 `x` / `x²` 项未乘 `B` |
| 4 | 变号、乘 `B` 都试了但**算积错** | 数字或 `x` / `x²` 系数错误 |

---

> **与旧版 L4 关系**：**废弃**「`±k1(…)±k2(…)` 双括号」「`abxy`、只展开不算积」等旧稿；现网 `buildExpandQuestion_L4` **待按本节重写**（可复用 L3 流程，扩展 `B` 与 `x²`、首项负号）。

---

## L5｜单项×括号 · 除号型 · 两括号相乘（三种外形）

**定位**：在 **L4** 之后；**取值与 L4 相同**：**`A、B、C、D` 等**可为**整数**，或 **`a、b、x、y` 中任意字母**，同一题可**混用**。共**三种题型**，抽题比例 **1∶1∶2**（即 **25%**、**25%**、**50%**）。**无括号嵌套**（每层括号内仍为 **`±` 连接两项**）。**本档不考虑「一次式」限制**（见下「题型③」）。

**出题逻辑**

1. **题型①**（占 **1/4**）：**`± A × (± B ± C)`**  
   整体最左可有 **`±`**；**`A`** 与**一层括号**之间为**乘法**（书写可用 **`×`** 或按现网省略乘号）；括号内为 **`± B ± C`**。**首项前的加号可省略**。

2. **题型②**（占 **1/4**）：**`(± B ± C) ÷ A`**（用**除号 `÷`** 表示，**不是**分数形式）  
   只有**分子**一侧带一层括号 **`(± B ± C)`**；**除号后面不再写括号**——**`A`** 为**单独一项**（整数或 **`a、b、x、y` 之一**），**不出现** **`÷ ( … )`**。理由：除号后再跟括号则「拆括号」训练意义不大，本档不出。  
   **本模式一律不用**分数线、竖式分数；**只用**一行式里的 **`÷`**。

3. **题型③**（占 **1/2**）：**`(± A ± B) × (± C ± D)`**（两括号**交叉相乘**）  
   **两个**一层括号相乘；每段括号内均为 **`±` 连接两项**。本题型**专门训练乘法分配律**（逐项叉乘），**不按**全局「未知数一次式」限制（展开后可出现 **`xy`** 等项）。

4. **抽题比例**：三种题型 **1∶1∶2**（**题型③** 为 **50%**）。

5. **取值**：**`A、B、C、D`** 规则与 **L4** 相同：**每格独立**为**整数**或 **`a、b、x、y` 之一**；**`a、b`** 为常数项，**`x、y`** 为未知数。

6. **核心**：  
   - **题型①**：**`A`** 乘入括号 → **分配**到 **`B`、`C`**，注意符号。  
   - **题型②**：**一行除法**书写；分子括号去括号规则 + 与除数 **`A`** 的衔接（**无**「分母括号」）。  
   - **题型③**：两括号相乘展开为**四项**；**正确答案**在系统中的**输出顺序**约定为 **AC → AD → BC → BD**（见「答案形式」，仅为格式统一，**不**表示别的顺序算错）。

**答案形式（L5）**

1. **只展开，不计算**；**不合并同类项**；不代入数值。
2. **题型③（两括号相乘）**：设第一括号为 **`(± A ± B)`**、第二括号为 **`(± C ± D)`**（**`A、B、C、D`** 为四项「位置」，各可为整数或字母）。展开后的**四个乘积项**，**标准答案（正确答案）**按固定顺序输出：**AC**、**AD**、**BC**、**BD**（即先 **`A`** 乘 **`C`、`D`**，再 **`B`** 乘 **`C`、`D`**；每项内部符号由展开规则决定，**不合并**）。  
   **说明**：该顺序**仅用于统一正确答案字符串/判对格式**；**代数上与正确答案等价**但**项的书写顺序不同**的展开，**不算数学错误**。**不要**用「四项顺序错了」来构造干扰项——顺序问题**不是**本模式要考的错因。
3. **本档**（尤其题型③）**不适用**「未知数必须一次式」的限制；以**分配律训练**为准。

**错因模板（L5 专用）**

1. 对于 `± A × (± B ± C)`，`± A` 分配时， 若`A` 前符号为负，负号被遗忘。
2. 对于 `± A × (± B ± C)`，`± A` 只乘括号内第一项。
3. 对于 `± A × (± B ± C)`，`± A` 只乘括号内第二项。
4. 对于 `± A × (± B ± C)`，分配时将`B` 或 `C` 的符号弄错。
5. 对于 `± A × (± B ± C)`，遗忘 `± A`，直接显示结果为 `± B ± C`。
6. 对于 `± A × (± B ± C)`，分配时运算符号错误，例如 `± A x (± B) ± A + (± C)` 或者 `± A + (± B) ± A x (± C)`。
7. 对于 `(± B ± C) ÷ A`，只除括号内第一项，第二项未除。
8. 对于 `(± B ± C) ÷ A`，只除括号内第二项，第一项未除。
9. 对于 `(± B ± C) ÷ A`，除法分配时将括号内第一项 `B` 前符号与第二项 `C` 前符号两者弄混。
10. 对于 `(± B ± C) ÷ A`，除法分配时将括号内第一项 `B` 前符号或第二项 `C` 前符号看错。
11. 对于 `(± A ± B) × (± C ± D)`，乘法分配时漏乘第一个或第二个括号中某项。
12. 对于 `(± A ± B) × (± C ± D)`，乘法分配时漏乘某一括号中某项。
13. 对于 `(± A ± B) × (± C ± D)`，乘法分配时改变第一个括号内某项符号（即 `A` 与 `B` 前的符号在分配时某一个被改变）。
14. 对于 `(± A ± B) × (± C ± D)`，乘法分配时改变第一个括号内整体符号（即 `A` 与 `B` 前的符号在分配时均被改变）。
15. 对于 `(± A ± B) × (± C ± D)`，乘法分配时改变第二个括号内某项符号（即 `C` 与 `D` 前的符号在分配时某一个被改变）。
16. 对于 `(± A ± B) × (± C ± D)`，乘法分配时改变第二个括号内整体符号（即 `C` 与 `D` 前的符号在分配时均被改变）。
17. 对于 `(± A ± B) × (± C ± D)`，漏掉交叉项（只做了 `AC` 和 `BD`）。
18. 对于 `(± A ± B) × (± C ± D)`，漏掉 `AC` / `AD` / `BC` / `BD` 中某一项（**一项**，非多项）。
19. 对于 `(± A ± B) × (± C ± D)`，弄错分配后 `AC` / `AD` / `BC` / `BD` 中某一项前的系数（**一项**，非多项）。

> **整合说明**：作者稿 L5 第 12 条原文「第个括号」已按语义改为「某一括号」；原稿中空条目未纳入。

---

## 其它（产品）

1. **升降级与局内规则**：仍以现网拆括号模式为准（如 20 题、即时讲解、Next、全对升 / 错多降等），除非产品另改。
2. **与管理端**：`docs/admin/` 等与关卡时间、模式名相关的配置，随关卡描述变更时一并核对。
3. **L5 两括号相乘（逻辑约束）**：干扰项不要用「和正确答案只是项的书写顺序不同、代数上仍一样」的式子当错项；项顺序只用于统一标准答案格式。

---

## 附录 · 程序实现（L1–L5）

> 供开发与 AI 对照正文改代码。**逻辑设计者只需看正文各档。**

### 共用机制

| 项目 | 说明 |
|------|------|
| 源文件 | `docs/shared/expand-brackets-questions.js`（与 `docs/index.html` 内联块需同源） |
| 入口 | `JmlExpandBrackets.buildQuestion(level)`：`level` **0～4** 对应 **L1～L5** |
| 选项组装 | `docs/index.html` 中先放入 1 个正确答案，再从 `wrongPool` **打乱后最多再取 3 个**错误项（共 4 选项） |
| 错因条数 | 规格：每题 **3** 条错因。生成器若 `wrongPool` **>3**，应**随机抽 3 条**再返回；现网部分档位（如 L3）为「全量入池、由 UI 截断」，与规格对齐时以本文为准改代码 |
| 去重 | 错误项文本不得与正确答案、题面相同；实现侧另有 `dedupeExpandWrongPool` 等 |
| 常用返回字段 | `expandKind`、`prompt`、`correctText` 或 `correctTerms`、`wrongPool[]`（`text`、`explain`、`causeNo`）、`presetWrong` |

**渲染**：L1 整数项用 `formatExpandedTerms`；含字母/乘除的档位用 `ebJoinSum`、`ebPairsToSum` 等（见各档函数）。

---

### L1 实现 · `buildExpandQuestion_L1`

| 步骤 | 逻辑 |
|------|------|
| 抽题型 | `l1PickQuestionType()`：**80%** `L1-T1`，**20%** `L1-T2` |
| 抽参 | `A,B,C ∈ [1,12]`；括号内加/减各 50%（`innerPlus`） |
| 题面 | T1：`A - (B ± C)`；T2：`A + (B ± C)` |
| 正确答案 | `l1CorrectTerms` → `formatExpandedTerms`，与正文表一致 |
| 错因 | `l1WrongPoolForType` 按题型生成 3 条 → `ebDedupeWrongPool` → `ebPickWrongPoolEntries(..., 3)`；`causeNo` 在题型内 **1～3** |
| 返回字段 | `expandKind: "L1"`、`questionType`、`prompt`、`correctTerms`、`correctText`、`wrongPool`、`presetWrong: []`；`outerMinus` 同 `questionType === "L1-T1"`（兼容旧读取） |

---

### L2 实现 · `buildExpandQuestion_L2`

| 步骤 | 逻辑 |
|------|------|
| 抽题型 | `l2PickQuestionType()`：权重 **20%、10%、20%、20%、20%、10%** → `L2-T1`～`L2-T6` |
| 抽参 | `k,A,B ∈ [1,12]`；`k` 左右各 50%（T3/T4 仅 `k` 在左）；内层 ± 或 ×÷ 各 50%（按题型） |
| 构建 | `l2BuildT1` … `l2BuildT6` → `prompt`、`correctText`、`fullWrongPool` |
| 错因 | `ebDedupeWrongPool` → `ebPickWrongPoolEntries(..., 3)`；`causeNo` **按题型** 1 起 |
| 返回字段 | `expandKind: "L2"`、`questionType`、`prompt`、`correctText`、`wrongPool`、`presetWrong: []` |

**要点**：T3 正确项为 `L2_CANNOT_REMOVE`（i18n「无法去括号」）；T4 内层 `×`→`k÷A÷B`，内层 `÷`→`k÷A×B`。

---

### L3 实现 · `buildExpandQuestion_L3`

| 步骤 | 逻辑 |
|------|------|
| 抽题型 | `l3PickQuestionType()`：**25%** × `L3-T1`～`T4`（外/内 ± 组合） |
| 抽参 | **`k` ∈ [2,12]**；括号内常数与 `x` 系数 **∈ [1,12]** 且互异；`A` 常数/`□x` 各 50%；布局 **80%** `A` 在左 |
| 题面 | `A ± k(B ± C)` 或 `k(B ± C) ± A`；无乘号 `×` |
| 正确答案 | `l3DistPair` → `l3OrderAnswer`（**仅** `A − k(…)` 时对分配两项变号；`k(…) − A` 时分配项不变号、末尾减 `A`）→ `l3JoinTerms`，不合并同类项 |
| 错因 | `l3WrongPoolForType`（含「第二项未乘 `k`」「两项都未乘 `k`」等）→ 去重 → 不足 3 条则重抽 → `ebPickWrongPoolEntries(..., 3)` |
| 返回字段 | `expandKind: "L3"`、`questionType`、`prompt`、`correctText`、`wrongPool`、`presetWrong: []` |

---

### L4 实现 · `buildExpandQuestion_L4`（待按正文重写）

| 步骤 | 逻辑 |
|------|------|
| 目标 | 通式 **`±A ± B(±C ± D)`**；四题型 **L4-T1～T4**（同 L3 占比）；**废弃**现网 L4a/L4b 双段逻辑 |
| 抽参 | `A`、`B`、`C`、`D` 规则见正文；**`B` 常数 2～12 或一次项系数 2～12**；**`A`/`C` 可首项负号**；允许 **`x²`** |
| 正确答案 | 在 L3 的 `l3OrderAnswer` 思路上将乘数改为 **`B`**（含一次项分配）；外减号规则同 L3 |
| 错因 | `l4WrongPoolForType`：以 **L3** 四型错因为底，`k`→`B`；`ebPickWrongPoolEntries(..., 3)` |
| 返回字段 | `expandKind: "L4"`、`questionType`（`L4-T1`～`T4`）、`prompt`、`correctText`、`wrongPool`、`presetWrong: []` |

---

### L5 实现 · `buildExpandQuestion_L5`

| 子档 | 占比 | `expandKind` | 说明 |
|------|------|--------------|------|
| a | 25% | `L5a` | `±A×(±B±C)` |
| b | 25% | `L5b` | `(±B±C)÷A`（除号后无括号） |
| c | 50% | `L5c` | `(±A±B)×(±C±D)`；正确答案四项顺序 **AC, AD, BC, BD** |

`L5c` 的 `wrongPool` 经 `dedupeExpandWrongPool`；`causeNo` 见源码（档内 1～19）。

---

## 修订记录

| 日期 | 说明 |
|------|------|
| 2026-04-02 | 助手整理：补全局约定、L1–L3 排版与 L3「首项前 ±」表述、补全 L4/L5 草案、其它与实现提示。 |
| 2026-04-02 | L1–L5 错因模板改为四列（1～4）对照表；L2/L4 第四列为「—」。 |
| 2026-04-02 | 取消横排列表：新增「错因模板全库」19 条一行一条；各级别改为逐条列出并引用全库编号。 |
| 2026-04-02 | 全局约定、L1–L5 出题说明、「其它」改为与错因相同的逐条列表；各级增加小标题「出题逻辑」。 |
| 2026-04-02 | L3 出题逻辑改为实现向表述：段间/段内 `±` 含义、数字为绝对值、书写省略正号、核心不变。 |
| 2026-04-02 | **L2 重写**：乘除去括号；括号与 `k` 仅乘除；括号内两数、`op` 可为 `+−×÷`；括号可在左或右。全库增 20–24。L4/L5 文案去掉对已废弃「L2 三项」的引用。 |
| 2026-04-02 | **L2 答案形式**：只拆括号不计算；分配/去括号变形；`( )÷k` 可除法分配拆开；`k÷( )` 正确照抄题面，错项用假分配。全库增 25。 |
| 2026-04-02 | L2：恢复「变号」用语并释义（×/÷ 运算符号规则，非泛称变形）；澄清 `(A×B)÷k` 仅一种正确展开，误拆为错解非二选一。 |
| 2026-04-02 | **L3**：运算规则不变；打破纯整数，`A～D` 可取整数或 **`a、b、x、y`**（ab 常数、xy 未知数）；只展开不计算。全局约定字母/展示同步调整。 |
| 2026-04-02 | **L3** 澄清：同一题可**混用多个字母与数字**（例 `(x+3)−(y−b)`），非整题只选一个字母。 |
| 2026-04-02 | **L4 重写**：混合 L1 型 **`±A±k(±B±C)`** 与 L3 型 **`±k1(±A±B)±k2(±C±D)`**；**`k、k1、k2`** 为正整数；**`A～D`** 同 L3（整数或 **`a、b、x、y`**）；首项正号可省略。全局约定 L4 与 L3 共用字母规则。 |
| 2026-04-02 | **L5 重写**：三种题型 **`±A×(±B±C)`**、**`(±B±C)/(±A)`**、**`(±A±B)×(±C±D)`**，比例 **1∶1∶2**；取值同 L4。全局：L5 纳入 **abxy**；**次数**补充第三型可能与 **`xy`** 项的关系。 |
| 2026-04-02 | **L5**：题型②改为 **`(±B±C)÷A`**（除号、非分数）；除号后**无括号**；题型③ 答案顺序 **AC、AD、BC、BD**；L5 **不考虑一次式**。全局去掉 L5 分数展示、次数与 **L5** 对齐。 |
| 2026-05-12 | **错因改版**：废除「错因模板全库」维护方式；并入《新拆括号等级说明.md》各档独立错因表；补充错因选取策略（适用集 >4 则随机 4）；L4 错因编号在本文内连续化；L5 修正作者稿笔误并删空条。 |
| 2026-05-25 | **结构改版**：全文改为「题型 + 错因」规格；重写开头与 **L1**（2 题型、档内 causeNo 1–6）；L2–L5 暂留旧版并标注待重写；不再引用《新拆括号等级说明.md》。 |
| 2026-05-25 | 干扰项改为每题 **3** 条错因；错因编号**按题型**从 1 起（L1 两型各 1–3）；正文与**附录**分离，L1–L5 实现归入附录。 |
| 2026-05-25 | **L2 正文定稿**：六题型 `L2-T1`～`L2-T6`（各占 1/6）、按题型错因表；T1/T5/T6 错因 >3 时抽 3；附录注明现网 `layout` 与 T4 缺口。 |
| 2026-05-25 | **L2-T4 更正**：`k÷(A×B)`、`k÷(A÷B)` **可去括号**；内层 ÷ 去括号后 **变 ×**（`k÷A×B`），内层 × 接成 `k÷A÷B`。 |
| 2026-05-25 | **L2 占比**：T1～T6 为 **20%、10%、20%、20%、20%、10%**。 |
| 2026-05-25 | **L2 实现**：`buildExpandQuestion_L2` 按六题型重构，对齐正文错因与 T4 变号规则。 |
| 2026-05-25 | **L3 正文重写**：改为 `A±k(B±C)` 简单代数式；仅 `x`、无负数、算积、无乘号选项；四题型各 25%；80/20 布局对调；`A` 与括号内项序各 50%。 |
| 2026-05-25 | **L3 错因**：T1/T2 以「某项忘了乘 `k`」（尤其第二项，如 `5+6+4x`）为主，去掉「漏整项」类错因。 |
| 2026-05-25 | **L3 错因**：错因 2 由「仅第一项未乘 `k`」改为「两项都未乘 `k`」（如 `5+3+4x`）。 |
| 2026-05-25 | **L3**：`k` 限定 **2～12**（不再出现 `k=1`），保证忘乘 `k` 错项可辨。 |
| 2026-05-25 | **L4 正文定稿**：`±A±B(±C±D)`；题型同 L3；仅 `x`、可 `x²`；**`A`/`C` 可首项负号**；**`B` 恒正**且常数 **2～12**；错因以 L3 为底；废弃双括号旧稿。 |
