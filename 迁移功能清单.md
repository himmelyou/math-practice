# calculation_v1-cursor-h5：从小程序迁回的升级改进清单



> 说明：下列条目对应微信小程序版 `JarvisMathLab` 中的增强功能；目标为在 H5（`docs/` + `server/server.js` + `admin/` + `report/`）中逐项对齐。  

> **最后更新**：与当前仓库代码对照后的状态。



---



## 已完成



### 计分与模式



- [x] **统一得分口径「每题固定 5 分」**  

  生存/闯关、训练模式与结算文案（积分 vs 正确题数）已按小程序思路区分。



- [x] **质数合数专项（Prime/Composite）**  

  - 前端：`docs/index.html` 完整流程 + `docs/prime-composite-logic.js`（与小程序逻辑对齐说明见脚本注释）。  

  - 记录：`runs` 写入 `mode: primeComposite`，服务端聚合用户最近记录等。  

  - **质数达人榜**：`server/server.js` 维护 `prime-perfect-ranking.json`，接口 `/api/prime-perfect-ranking`；H5 排行榜「质数达人」Tab 已接表。



### 管理端与报表



- [x] **管理端** `admin/`（替代旧 `admin.html`）  

  账号 / 关卡 / 头像 / 备份等 Tab；门禁 Gate UI；对接 `/api/admin/*`。



- [x] **报表页** `report/`（替代旧 `report.html`）  

  挑战记录、错题本、数据分析；`stats-*-browser.js` 等浏览器侧聚合与图表。



- [x] **启动与本地服务**  

  本地入口指向新路径（如 `local-admin-server.js`、一键启动脚本兼容 `admin/`、`report/`、`docs/`）。



### 头像



- [x] **服务端**：`avatars.json`、`avatar-assets/`、`/api/admin/avatars*`、`/avatar-assets` 静态资源；用户资料中的 `avatarId` 与排行榜 `avatarUrl` 等。  

- [x] **管理端**：头像上传、替换、删除、排序、解锁分等（见 `admin/admin-panel.js`）。  

- [x] **H5 主站**：头像选择页、首页与排行榜中展示真实头像（非仅占位）。



### 排行榜



- [x] **总积分榜**：`/api/score-ranking`，Tab 已接表。  

- [x] **生存榜**：`/api/survival-ranking`，含未通关 Gate；Tab 已接表。  

- [x] **质数达人榜**：见上文质数全对最短用时。

- [x] **耐力榜（streak）**：前端已接表；后端已提供 `/api/streak-ranking`；用户字段 `streakCurrent/streakBest/streakLastDate` 已增量维护，并提供管理端一次性回填。

- [x] **连击榜（combo）**：前端已接表；后端已提供 `/api/combo-ranking`；用户字段 `comboCurrent/comboBest` 已增量维护，并提供管理端一次性回填。



### UI 与其它



- [x] 首页入口分组、排行榜多 Tab（含耐力/连击实装）。  

- [x] 总积分榜 / 生存榜 / 质数达人榜表格样式与质数达人列对齐等近期 UI 调整。



---



## 尚未迁移 / 待补齐



- [ ] **与小程序逐项 diff**  

  若小程序后续仍有小功能（分享、订阅消息、微信专属 API 等），H5 侧不迁移属正常；有业务需要的再单列任务。



---



## 建议自测（发布前）



- 生存 / 闯关 / 训练 / 质数合数：答对加分是否为 **+5**，结算是否同时展示 **分** 与 **正确题数**（或等价统计）。  

- 登录后：头像在首页、排行榜、头像选择页是否一致；管理端改头像库后用户端是否可见。  

- 排行榜：**总积分 / 生存 / 质数达人 / 耐力 / 连击** 在无网/未登录时的提示是否合理。

- 部署：**`JWT_SECRET`、`DATA_DIR` 持久卷** 勿随意变更；重要 JSON 先备份再发版。



---



## 备注



- 持久化为 **`DATA_DIR` 下 JSON 文件**，不是 SQL；旧数据缺新字段时，服务端多以默认值兼容（参见 `server/server.js` 读写逻辑）。  

- 旧单文件 **`admin.html` / `report.html`** 若已删除，请以 **`admin/index.html`、`report/index.html`** 为准。


