# Jarvis Math Lab · 小数运算练习 · 难度等级说明

> 本文档描述小数运算模块 **D1～D5** 的出题逻辑（目标设计），供前后端实现与后续扩展参考。  
> 与整数四则（L1～L16）、拆括号（L1～L5）为**独立练习**；实现见 `docs/shared/decimal-questions.js`。

**通用约定**：

- 答案为 **非负有限小数**（除法生成时保证可除尽，不出现循环小数）。
- 减法按 **数值** 保证 **被减数 ≥ 减数**。
- **有效数字**按常规定义；**末尾 0** 仅在小数点后有数字时可计入有效位（如 `3.40` 为两位有效数字）。
- 书写统一用 **小数点**（如 `3.5`），不用分数式。
- **段内不重复**：训练 30 题 / 生存·闯关段 / 管理页每页，段初重置牌库或去重集，同段内尽量不重复题干。
- **不含 0 作为操作数**（D1～D5 均不出现加数、减数、乘数、除数为 `0` 的题）。

当前共 **5 级（D1～D5）**，按「加减混算 → 移位 → 乘除整数 → 乘除小数」递进。学员假定已具备整数四则基础，**首版不设更细的「纯 0.3+0.4」热身级**。

**建议解锁**：完成整数 **L10（乘法口诀）** 后开放 D1；D2～D5 按闯关进度逐级解锁（与整数线解耦）。

---

## D1 · 一位小数与整数混合加减

**说明**：一位小数与整数（或两位小数与整数）混合加减；操作数控制为 **1～2 位有效数字**，答案可进位到更多位数。

**出题规则**：

- 运算：**加法、减法各约 50%**
- 操作数（**不含 0**）：
  - **一位小数**：`0.1～9.9`（恰 1 位小数，天然为 1～2 位有效数字）
  - **整数**：`1～99`（`1～9` 为 1 位有效数字，`10～99` 为 2 位有效数字）
- 组合：小数 ± 小数、小数 ± 整数 均可能出现
- 减法：保证 **被减数 ≥ 减数**
- **答案**：**不限制**小数位数或有效数字个数（可为 3 位有效数字等），须为 **非负有限小数**
- 示例：`3.5 + 2 = ?`、`0.7 + 0.8 = ?`、`9.9 + 1.5 = 11.4`、`9 − 1.3 = ?`、`5.2 − 3.1 = ?`

---

## D2 · 一位小数与两位小数混合加减

**说明**：**仅小数**参与运算（**不允许整数**操作数）；在 D1 基础上练小数点对齐与不同小数位数的加减。

**出题规则**：

- 运算：**加法、减法各约 50%**
- 操作数（**不含 0**）：
  - 每个数 **最多 2 位有效数字**
  - **小数点后最多 2 位**；允许 **无效 0**（如 `3.40`、`8.50`）
  - **不允许整数**形操作数（题面中两数均带小数点）
- 题型占比（按两个操作数的小数位数）：
  - **25%**：**一位小数 + 一位小数**（如 `3.5 + 0.8`）
  - **50%**：**一位小数 + 两位小数**（如 `1.2 + 3.40`）
  - **25%**：**两位小数 + 两位小数**（如 `3.40 + 1.20`）
- 「一位小数」指题面恰 **1 位小数**；「两位小数」指题面恰 **2 位小数**（含末尾 0）
- 减法：保证 **被减数 ≥ 减数**
- **答案**：**非负有限小数**；**最多 2 位小数**
- 示例：`1.2 + 3.40 = ?`、`8.50 − 1.20 = ?`、`0.5 + 0.80 = ?`

> **实现提示**：生成两位小数时勿抽成超过 2 位有效数字的数（如 `1.25` 为 3 位有效数字，本档排除）。

---

## D3 · 乘或除以 10 / 100 / 1000 / 10000

**说明**：专练 **小数点移位**（或整数末尾加 0）；数值范围较 D1、D2 明显放大。

**出题规则**：

- 运算：**乘法、除法各约 50%**
- 乘数 / 除数：**10、100、1000、10000**（可均匀或略偏重 10、100）
- 题目中的另一操作数（**不含 0**）：
  - 数值范围 **0.0001～99999**
  - **最多 5 位有效数字**
- **答案**：
  - 数值范围 **0.0001～99999**
  - **最多 5 位有效数字**
  - **有限小数**
- 示例：`3.45 × 100 = ?`、`0.56 × 1000 = ?`、`560 ÷ 100 = ?`、`3.2 ÷ 10 = ?`、`5000 ÷ 10000 = ?`

---

## D4 · 小数 × 一位整数 或 ÷ 一位整数

**说明**：小数与 **2～9** 的一位整数相乘或相除；通过被乘数 / 被除数的 **有效数字位数** 控制心算难度。

**出题规则**：

- 运算：**乘法、除法各约 50%**
- 整数因子：**2～9**（一位整数，**不含 0、1**）
- 小数操作数（**不含 0**）按 **有效数字位数** 加权抽取：
  - **25%**：**3 位有效数字**
  - **50%**：**2 位有效数字**
  - **25%**：**1 位有效数字**
- 除法：保证 **商为有限小数**
- **答案**：**非负有限小数**
- 示例：`2.4 × 3 = ?`、`7.5 ÷ 5 = ?`、`12.5 × 4 = ?`

---

## D5 · 小数 × 小数 或 ÷ 小数

**说明**：第二个操作数仅 **1 位有效数字**，便于心算；第一个操作数为 2～3 位有效数字。

**出题规则**：

- 运算：**乘法、除法各约 50%**
- 第一个操作数（**不含 0**）按 **有效数字位数** 抽取：
  - **50%**：**3 位有效数字**
  - **50%**：**2 位有效数字**
- 第二个操作数（**不含 0**）：**恰 1 位有效数字**，形如 **d × 10^k**（**d ∈ {1,2,…,9}**，**k** 为整数）
  - 例：`0.2`、`0.05`、`3`、`40` 可；`0.25`、`1.23` 不可
- 除法：保证 **商为有限小数**（生成时约束除数因子或先化整，避免循环小数）
- **答案**：**非负有限小数**
- 示例：`1.2 × 0.3 = ?`、`0.8 ÷ 0.2 = ?`、`15 × 4 = ?`（第二个数为 4 时）

---

## 难度递进概览

| 级别 | 主题 | 主要运算 | 操作数要点 |
|------|------|----------|------------|
| D1 | 一位小数与整数混合加减 | +、− | 1～2 位有效数字；含整数；答案不限位数 |
| D2 | 一位与两位小数加减 | +、− | 仅小数；1+1 / 1+2 / 2+2 占比 25% / 50% / 25% |
| D3 | ×10ⁿ / ÷10ⁿ | ×、÷ | 0.0001～99999，≤5 位有效数字 |
| D4 | 小数 ×、÷ 一位整数 | ×、÷ | 小数 1～3 位有效数字（25% / 50% / 25%） |
| D5 | 小数 ×、÷ 小数 | ×、÷ | 第一个 2～3 位有效数字；第二个 1 位有效数字 |

---

## 后续扩展（未定稿）

| 级别 | 主题 |
|------|------|
| D6（拟） | 无括号四则混合（对标整数 L15） |
| D7（拟） | 带括号四则混合（对标整数 L16） |

---

## 与整数线的关系

| 整数级 | 小数级建议衔接 |
|--------|----------------|
| L10 乘法口诀 | 开放 D1 |
| L11 两位 ÷ 一位 | D4 除法心智模型 |
| L15 无括号混合 | D6（若实现） |
| L16 带括号 | D7（若实现） |

---

*最后更新：2026 年*
