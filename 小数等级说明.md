# Jarvis Math Lab · 小数运算练习 · 难度等级说明

> 本文档描述小数运算模块 **D1～D5** 的出题逻辑（目标设计），供前后端实现与后续扩展参考。  
> 与整数四则（L1～L16）、拆括号（L1～L5）为**独立练习**；实现见 `docs/shared/decimal-questions.js`。

**通用约定**：

- 答案为 **非负有限小数**（除法生成时保证可除尽，不出现循环小数）。
- 减法按 **数值** 保证 **被减数 ≥ 减数**。
- **有效数字**按常规定义；**末尾 0** 仅在小数点后有数字时可计入有效位（如 `3.40` 为两位有效数字）。
- 书写统一用 **小数点**（如 `3.5`），不用分数式。
- **段内不重复**：训练 30 题 / 生存·闯关段 / 管理页每页，段初重置牌库或去重集，同段内尽量不重复题干。
- **不含 0 作为操作数**（D1～D5 均不出现加数、减数、乘数、除数为 `0` 的题）。

当前共 **5 级（D1～D5）**，按「加减混算 → 移位 → 乘除整数 → 乘除小数」递进。学员假定已具备整数四则基础，**首版不设更细的「纯 0.3+0.4」热身级**。

**建议解锁**：完成整数 **L10（乘法口诀）** 后开放 D1；D2～D5 按闯关进度逐级解锁（与整数线解耦）。

---

## D1 · 一位小数与整数混合加减

**说明**：一位小数与整数（或两位小数与整数）混合加减；操作数控制为 **1～2 位有效数字**，答案可进位到更多位数。

**出题规则**：

- 运算：**加法、减法各约 50%**
- 操作数（**不含 0**）：
  - **一位小数**：`0.1～9.9`（恰 1 位小数，天然为 1～2 位有效数字）
  - **整数**：`1～99`（`1～9` 为 1 位有效数字，`10～99` 为 2 位有效数字）
- 组合：**75%** 小数 ± 小数、**25%** 小数 ± 整数（左右顺序各约 50%；**不含**整数 ± 整数）
- 减法：保证 **被减数 ≥ 减数**
- **答案**：**不限制**小数位数或有效数字个数（可为 3 位有效数字等），须为 **非负有限小数**
- 示例：`3.5 + 2 = ?`、`0.7 + 0.8 = ?`、`9.9 + 1.5 = 11.4`、`9 − 1.3 = ?`、`5.2 − 3.1 = ?`

---

## D2 · 一位小数与两位小数混合加减

**说明**：**仅小数**参与运算（**不允许整数**操作数）；在 D1 基础上练小数点对齐与不同小数位数的加减。

**出题规则**：

- 运算：**加法、减法各约 50%**
- 操作数（**不含 0**）：
  - 每个数 **最多 2 位有效数字**
  - **禁止末尾无效 0**（题面不写 `3.40`、`8.50` 等；展示为 `3.4`、`8.5`）
  - **不允许整数**形操作数（题面两数均须含小数点）
- 一位小数操作数：**0.1～9.9**（实质只有十分位）
- 两位小数操作数：**0.01～0.99**，且 **百分位为 1～9**（如 `0.56`、`0.08`）
- 题型占比（按两个操作数的小数位数）：
  - **25%**：**一位小数 + 一位小数**（如 `3.5 + 0.8`）
  - **50%**：**一位小数 + 两位小数**（如 `1.2 + 0.56`）
  - **25%**：**两位小数 + 两位小数**（如 `0.56 + 0.78`）
- 减法：保证 **被减数 ≥ 减数**
- **答案**：**非负有限小数**；**最多 2 位小数**，同样 **去掉末尾无效 0**
- 示例：`1.2 + 0.56 = ?`、`8.5 − 0.12 = ?`、`0.5 + 0.08 = ?`

> **实现提示**：两位小数操作数实质在 **0.01～0.99** 且百分位非零；勿生成超过 2 位有效数字的数（如 `1.25`）。

---

## D3 · 乘或除以 10 / 100 / 1000 / 10000

**说明**：专练 **小数点移位**（或整数末尾加 0）；数值范围较 D1、D2 明显放大。

**出题规则**：

- 运算：**乘法、除法各约 50%**
- 乘数 / 除数：**10、100、1000、10000**；在使答案合法的幂次中**均匀抽取**（不盲目随机）
- 第一个操作数（**不含 0**）：
  - **25% / 50% / 25%** → **3 / 2 / 1 位有效数字**（末位 **1～9**）
  - 题面形态（各约 1/7）：**4 / 3 / 2 / 1 位小数、整数、×10、×100**（`M / 10^dp` … `M×100`）
- **答案**：
  - **0 < 答案 < 99999**（最多 4 位小数已隐含 **≥ 0.0001**）
  - **整数**，或**最多 4 位小数**（去尾零）
- 示例：`2.4 × 100 = ?`、`0.56 × 1000 = ?`、`560 ÷ 100 = ?`、`3.2 ÷ 10 = ?`、`24 × 100 = ?`

---

## D4 · 小数 × 一位整数 或 ÷ 一位整数

**说明**：小数与 **2～9** 的一位整数相乘或相除；先定乘除，再生成操作数（与 D5 流程一致，第二个操作数仅为整数）。

**出题规则**：

- 运算：**乘法、除法各约 50%**（先定乘除）
- 整数因子：**2～9**（一位整数，**不含 0、1**）
- 第一个操作数（**不含 0**；仅 **1 / 2 / 3 位小数**题面，无整数、无 ×10）：
  - **乘法**：**50%** 一位有效数字、**50%** 两位有效数字
  - **除法**：**25% / 50% / 25%** → **1 / 2 / 3 位有效数字**
  - 有效数字末位 **1～9**；形态为 **1 / 2 / 3 位小数**（`M / 10^dp`）均匀随机
- **乘法**：`第一个 × n = ?`，答案为积
- **除法**：`第一个 ÷ n = ?`，答案为商（`n ∈ {3,6,7,9}` 时微调 **M** 保证除尽；`n ∈ {4,8}` 时商有效数字 **>4** 时微调 **M**）
- **答案**：**非负有限小数**
- 示例：`2.4 × 3 = ?`、`7.5 ÷ 5 = ?`、`0.56 ÷ 2 = ?`

---

## D5 · 小数 × 小数 或 ÷ 小数

**说明**：第二个操作数仅 **1 位有效数字**；第一个操作数有效数字位数随乘/除而异。两操作数题面各有五种形态。

**出题规则**：

- 运算：**乘法、除法各约 50%**（先定乘除，再生成操作数）
- 第一个操作数（**不含 0**）：
  - **乘法**：**50%** 一位有效数字、**50%** 两位有效数字
  - **除法**：**25% / 50% / 25%** → **1 / 2 / 3 位有效数字**
  - 题面形态（各约 20%）：**3 位小数 / 2 位小数 / 1 位小数 / 整数 / ×10**（有效数字末位 **1～9**）
- 第二个操作数（**不含 0**）：**恰 1 位有效数字**（`d ∈ {1,…,9}`）；五种题面形态同上；**`d = 1` 时不可为整数 `1`**
- **乘法**：先抽第一个，再抽 `d`；在使积 ∈ **0.0001～9999** 的形态中均匀抽第二个
- **除法**：先抽第一个，再抽 `d`；`first ÷ second = ?`
  - `d ∈ {3,6,7,9}`：将第一个的 **M** 调到最近的 **d** 的倍数（先试较小，末位为 0 则用较大），保证除尽
  - `d ∈ {4,8}`：商有效数字 **>4** 时同样微调 **M**；筛形态时排除商 >4 位有效数字
  - `d ∈ {1,2,5}`：不微调 **M**（商有效数字自然 ≤4）
  - 在使商 ∈ **0.0001～9999** 的第二个形态中均匀抽取
- **答案**：**非负有限小数**，且 ∈ **0.0001～9999**
- 示例：`1.2 × 0.3 = ?`、`2.4 ÷ 0.2 = ?`、`0.024 × 50 = ?`

---

## 难度递进概览

| 级别 | 主题 | 主要运算 | 操作数要点 |
|------|------|----------|------------|
| D1 | 一位小数与整数混合加减 | +、− | 1～2 位有效数字；含整数；答案不限位数 |
| D2 | 一位与两位小数加减 | +、− | 仅小数；1+1 / 1+2 / 2+2 占比 25% / 50% / 25% |
| D3 | ×10ⁿ / ÷10ⁿ | ×、÷ | 1～3 位有效数字；7 档题面；答案 < 99999、≤4 位小数 |
| D4 | 小数 ×、÷ 一位整数 | ×、÷ | 乘：第一个 1～2 位；除：第一个 1～3 位；仅 3 档小数题面 |
| D5 | 小数 ×、÷ 小数 | ×、÷ | 乘：第一个 1～2 位有效数字；除：第一个 1～3 位（25/50/25） |

---

## 后续扩展（未定稿）

| 级别 | 主题 |
|------|------|
| D6（拟） | 无括号四则混合（对标整数 L15） |
| D7（拟） | 带括号四则混合（对标整数 L16） |

---

## 与整数线的关系

| 整数级 | 小数级建议衔接 |
|--------|----------------|
| L10 乘法口诀 | 开放 D1 |
| L11 两位 ÷ 一位 | D4 除法心智模型 |
| L15 无括号混合 | D6（若实现） |
| L16 带括号 | D7（若实现） |

---

*最后更新：2026 年*
