/**
 * API 服务器地址配置
 * - 本地开发：npm run start 启动 server 后，使用 http://localhost:3001
 * - 线上部署：将下方地址改为你部署的后端地址（如 Railway、Render 等）
 */
window.API_BASE_URL = window.API_BASE_URL || "http://localhost:3001";
