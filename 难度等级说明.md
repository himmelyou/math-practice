# Jarvis Math Lab 计算游戏 · 难度等级说明

> 本文档描述当前版本的 16 个难度等级的出题逻辑，供前后端实现与后续扩展参考。  
> 说明为「目标设计」，实际代码实现应尽量对齐本规则，个别细节如需调整，可在此文中同步更新。

**通用约定**：

- 所有数值均为整数，结果要求为 **非负整数**。
- 若未特别说明，数值区间为闭区间（例如「1～9」表示 1 ≤ n ≤ 9）。
- 为控制整体难度，题库生成时会进行随机抽样，并按比例控制：
  - 是否进位 / 退位
  - 不同题型（加减乘除）的占比
- **段内不重复**：每局训练（20 题牌库，全对即结束；错 1 题再加 10 题，错 2 题及以上即结束）、生存/闯关段（每关 20 题牌库，先出 10 题、必要时再用后 10 题）、管理页每页打印，在段初重置牌库或去重集，同段内尽量不出现相同题干。

当前共 16 级（L1～L16），按照「一位数 → 两位数 → 乘除 → 四则混合 → 括号」的路径渐进。

---

## L1 · 一位数加法入门

**说明**：1～9 的一位数加法，无进位与进位各半，适合作为热身和基础熟练。

**出题规则**：
- 运算：加法
- 范围：1～9 + 1～9（无序；a+b 与 b+a 视为同一题）
- **不进位库**（a+b≤9）20 种、**进位库**（a+b≥10）25 种
- 每局/每页按题量各取约 50%（奇数时多出的 1 题归进位库），合并洗牌，段内不重复
- 每题展示时两数顺序随机
- 示例：3 + 4 = ?、6 + 7 = ?

---

## L2 · 20 以内加法

**说明**：1～10 与 10～19 相加，和不超过 20，衔接一位数与两位数加法。

**出题规则**：
- 运算：加法
- 一个加数 1～10，另一个 10～19，且 a+b≤20（无序，共 55 种）
- 洗牌后按段题量抽取，段内不重复；展示时两数顺序随机（10+10 除外）
- 示例：3 + 12 = ?、10 + 10 = ?、1 + 19 = ?

---

## L3 · 一位数加减混合

**说明**：9 以内加减混合，巩固「凑整」和「退整」概念，训练在同一局中切换运算类型。

**出题规则**：
- 运算：加法、减法各约 50%
- 范围：1～9
- **加法库** 25 种（1～9 无序、和 ≥ 10 的进位加法）、**减法库** 39 种（被减数 4～9，减数 ≤ 被减数）
- 每局/每页按题量各取约 50%（奇数时多出的 1 题归减法库），合并洗牌，段内不重复
- 加法展示时两数顺序随机
- 示例：7 + 5 = ?、9 − 4 = ?

---

## L4 · 两位数加减基础

**说明**：20 以内两位数加减，包含简单进退位，是从一位数运算过渡到两位数笔算的桥梁。

**出题规则**：
- 运算：加法、减法各约 50%
- **加法**：a 在 6～15，b 在 4～9，结果 ≤ 20（共 50 种）
- **减法**：a 在 11～20，b 在 2～9（共 80 种）
- 每局/每页按题量各取约 50%（奇数时多出的 1 题归减法库），合并洗牌，段内不重复
- 示例：12 + 7 = ?、18 − 9 = ?

---

## L5 · 两位数加一位数 / 整十数

**说明**：两位数加一位数或整十数，整十数题型用于巩固「十位 / 个位」位值，一位数题型侧重进位。

**出题规则**：
- 运算：加法
- 整十数 : 一位数 ≈ 4 : 6
- **整十数**：b 为 10、20、…、90，如 35 + 20 = ?
- **一位数**：b 为 4～9，约 75% 保证个位进位，如 27 + 6 = ?
- 段内按题干去重，尽量不重复
- 示例：35 + 20 = ?、27 + 6 = ?

---

## L6 · 两位数减一位数 / 整十数

**说明**：两位数减一位数或整十数，整十数题型巩固位值，一位数题型侧重退位。

**出题规则**：
- 运算：减法
- 整十数 : 一位数 ≈ 4 : 6
- **整十数**：b 为 10、20、…、90，且 a ≥ b，如 56 − 20 = ?
- **一位数**：约 75% 保证退位，如 32 − 6 = ?
- 段内按题干去重，尽量不重复
- 示例：56 − 20 = ?、32 − 6 = ?

---

## L7 · 两位数加两位数（进位）

**说明**：两位数加两位数，进位为主，结果不超过 100，与 L8 的退位减法相互呼应。

**出题规则**：
- 运算：加法
- 范围：a、b 均为 10～99，且 a + b ≤ 100
- 约 80% 题目保证有至少一位进位（个位或十位）
- 段内按题干去重，尽量不重复
- 示例：47 + 38 = ?（85）、56 + 29 = ?（85）

---

## L8 · 两位数减两位数（退位）

**说明**：两位数减两位数，退位为主，巩固「借一当十」的竖式减法思路。

**出题规则**：
- 运算：减法
- 范围：20～99
- 约 80% 题目保证退位
- 段内按题干去重，尽量不重复
- 示例：52 − 36 = ?、81 − 47 = ?

---

## L9 · 两位数加减混合

**说明**：两位数加减混合，进位与退位交替出现，结果不超过 100，要求学生在同一局中频繁切换运算策略。

**出题规则**：
- 运算：加法、减法各约 50%
- 范围：a、b 均为两位数，加法结果 ≤ 100（可等于 100），减法结果自然 ≤ 99
- 加法约 70% 进位；减法约 70% 退位
- 段内按题干去重，尽量不重复
- 示例：63 + 28 = ?（91）、50 + 50 = ?（100）、91 − 57 = ?

---

## L10 · 乘法口诀基础

**说明**：2 到 9 的乘法口诀（不含 ×1），打牢乘法基础。

**出题规则**：
- 运算：乘法
- 范围：2～9 × 2～9（无序，共 36 种搭配；a×b 与 b×a 视为同一题）
- 从 36 种搭配中洗牌后抽取所需题量，同局/同页内不重复
- 每题展示时两数顺序随机（如 6×7 或 7×6）
- 示例：6 × 7 = ?、4 × 9 = ?

---

## L11 · 两位除一位整除

**说明**：两位数除以一位数，结果为整数，是乘法口诀的逆运算，强调「乘除互逆」。

**出题规则**：
- 运算：除法
- 除数：2～9，商：2～9，且 **除数 ≠ 商**（共 56 种）
- 被除数 = 除数 × 商（整除）
- 洗牌后按段题量抽取，段内不重复
- 示例：42 ÷ 6 = ?、56 ÷ 7 = ?（不含 36 ÷ 6 这类除数与商相同者）

---

## L12 · 两位数加两位数（结果超 100）

**说明**：两位数加两位数，结果超过 100，引入三位数加法，为后续乘法和更高位计算打基础。

**出题规则**：
- 运算：加法
- 范围：a、b 均为 10～99，且 a + b > 100
- 约 80% 题目保证进位
- 段内按题干去重，尽量不重复
- 示例：78 + 56 = ?（134）、65 + 48 = ?（113）

---

## L13 · 两位乘一位

**说明**：两位数乘一位数，结果不超过 100，为多位数乘法竖式打基础，强化「分配律」直觉。

**出题规则**：
- 运算：乘法
- a：11～99（两位数），b：2～9（一位数）
- 约束：a × b ≤ 99（共 98 种）
- 洗牌后按段题量抽取，段内不重复
- 示例：24 × 3 = ?、12 × 7 = ?

---

## L14 · 两位乘一位的逆运算

**说明**：两位数或三位数除以一位数，商为两位数，对应「两位乘一位」的逆运算。

**出题规则**：
- 运算：除法
- 除数：2～9，商：11～99（两位数）
- 被除数 = 除数 × 商（整除）
- 段内按题干去重，尽量不重复
- 示例：84 ÷ 7 = ?、108 ÷ 4 = ?

---

## L15 · 不带括号的四则运算

**说明**：3 个数 2 个运算符，覆盖四则混合的 **12 种**有效运算符组合（去掉连加、连减、连乘、连除四种），单步难度约至 L11（不高于前面乘除难度）。

**出题规则**：
- 运算：+、−、×、÷ 混合，**不含** `++`、`−−`、`××`、`÷÷` 四种连算
- **题型占比**：50% 为「先算后面」（前加减、后乘除，如 `a + b × c`）；50% 为「先算前面」（其余 8 种，如 `a × b + c`、`a + b − c`、`a × b ÷ c`）
- 乘法：2～9 × 2～9；除法：两位数÷一位数=一位数商，保证整除
- 所有中间结果与最终答案为非负整数
- 段内按题干去重，尽量不重复
- 示例：3 + 4 × 5 = ?、20 − 12 ÷ 4 = ?、8 × 2 + 3 = ?、15 + 6 − 4 = ?

---

## L16 · 带括号的四则运算

**说明**：3 个数 2 个运算符，**12 种**有效组合（与 L15 同为 12 种外形：L15 含 `+−`、`×÷`，L16 换为 `−−`、`÷÷`，因后两者加括号会改变求值）。约 **70%** 题目加一对括号以改变运算顺序，约 **30%** 无括号作混淆、并复习 L15。单步难度约至 L11。

**出题规则**：
- **12 种运算符组合**（`1/12` 均匀随机）：先算后面的 4 种（`+×`、`+÷`、`−×`、`−÷`）+ 先算前面的 8 种（`−+`、`×+`、`×−`、`÷+`、`÷−`、`÷×`、`−−`、`÷÷`）
- **括号策略**：70% 加括号——先算后面的在左侧 `(a±b) op c`，先算前面的在右侧 `a op (b op c)`；30% 不加括号
- 有括号时，**有括号与无括号的答案必须不同**；所有**中间结果**与最终答案为非负整数；除法保证整除（反推构造）
- 段内按题干去重，尽量不重复
- 示例：`(3 + 4) × 5 = ?`、`20 ÷ (2 + 2) = ?`、`18 − 6 − 4 = ?`、`48 ÷ (6 ÷ 2) = ?`

---

## 难度递进概览

| 级别      | 主题                           | 主要运算 |
|-----------|--------------------------------|----------|
| L1～L3    | 一位数加减                     | +、−     |
| L4～L9    | 两位数加减（结果 ≤ 100）       | +、−     |
| L10       | 乘法口诀                       | ×        |
| L11       | 除法入门（两位除一位整除）     | ÷        |
| L12       | 两位数加两位数（结果 > 100）   | +        |
| L13～L14  | 两位乘一位及其逆运算           | ×、÷     |
| L15～L16  | 四则运算（无括号 / 带括号）    | +、−、×、÷ |

---

*最后更新：2025 年*
