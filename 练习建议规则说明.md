# Jarvis Math Lab · 练习建议规则说明

> 面向家长助手 / 今日练习建议。  
> **当前状态：v0.21 未完成 1～5 都能开练、对上就跟局**（其余同 v0.20：不整单重排；失败进冷却、不后插；速度任务须准≥95%；放弃局不算；均速只算对题；冷却窗不清）。report 未完成 / 冷却各五个方块。对照用，不改训练开局。  
> 案例原文见《练习建议案例.md》。规则版本：`0.21-provisional`。

本文档是规则规格源；案例细节以案例文档为准。代码落地后若行为与本文不一致，以本文为准并改代码。

**勿忘（v0.21）：** 不要整单重排未完成五格。1～5 都能开练，对上哪条跟哪条（点按钮或首页碰巧同一套，未达也记窗）。成功/失败少一格才补一格，剩下四格不动；规则升级不拆队。名单外的局跟完后才重算未完成（已有尝试的关保留）；点「重新开单」才丢掉未完成进度。详见 §七 R-plan、§十「未完成何时动」。

---

## 一、我们怎么一起把规则磨出来

1. 你在对话里丢一个**案例**（按第八节模板，不必一次填全）。
2. 我用本机脚本拉该学员的**信号快照**（缺数据会标出来）：

```text
node server/scripts/coach-snapshot.js 用户名
```

口令在仓库根目录 `.env` 的 `JML_ADMIN_PIN`（与 report 门禁相同，文件已 gitignore，勿提交）。校验：`node server/scripts/coach-snapshot.js --check`。
3. 你确认或修正「今天该练什么」。
4. 我把这条判断收进 **§七 决策树** 和 **§十 可执行规则**，必要时改阈值。
5. 下一条案例用来打补丁：新情况就加规则，和旧案例打架就先写进「冲突」再一起裁定。

一条案例尽量只讲**一个决策点**（例如「断练三天还要不要冲关」），不要一次塞进所有模式。

可以直接这样说：

```
案例：学员 xxx
我的判断：今天应该……
原因：……
不要：……
```

用户名、年级、断练几天、热图观感都可以附上。没有用户名时用「虚拟：一年级、断练 5 天、L6 橙区」也行。

---

## 二、目标与边界

### 最终形态（v2，暂不实现）

主页角色给出今日练习，**用助手选关取代训练模式现有 F/H 选关**（一键按建议开局）。  
**不把年级、熟练度等塞进现有 `computeTrainingNextLevel`**，避免把训练引擎改复杂。

### 中间形态（v1，暂不实现）

同一套建议出现在主页，仍是展示；训练开局暂时仍走旧引擎。

### 当前要做的（v0）

- 只在 **report → 数据分析顶部** 输出建议（详细建议 + 原因）。
- **不改** 训练 / 特殊模式开局代码。
- 建议与「旧训练选关」并排，只为对照，不表示长期双轨。

### 明确不做（直到案例证明需要）

- 用大模型临场决定练什么（规则必须可复现、可解释）。
- 从对错答案自动诊断「进位错 / 看错符号」等错因，并据此改关（v0 最多当展示）。
- 用真实年龄（账号里没有生日，只有管理端年级）。

---

## 三、和现有选关的关系

现有引擎只回答：**今天四则训练打哪一关**（前沿日 F / 热图日 H）。

| 层 | 职责 | 现状 |
|---|---|---|
| 选关层 | 四则训练 L 几、前沿还是刷热图 | 已有 `computeTrainingNextLevel` |
| 教练层（本文） | 今天练哪一种模式、强度、要不要覆盖选关、怎么跟家长说 | **待案例标定** |
| 角色层 | 主页人设与一键开练 | 以后 |

**与旧选关的关系：**

- 旧引擎继续跑，作为对照和过渡开局。
- 助手按案例磨自己的规则；与旧选关不一致是预期（见案例 01），不是要去改旧引擎。
- 默认不再写「助手 = 旧选关」。磨熟后开局只听助手。

现有选关原因码（实现已有，助手文案应对齐，不要另造一套）：

| 原因码 | 含义（简述） |
|---|---|
| `frontier_stabilize_M` | 前沿日：稳梯子顶（加权未达 95% 或未流畅） |
| `frontier_stabilize_slow` | 前沿日：顶已准但过慢 |
| `frontier_open_M1` | 前沿日：顶已流畅，开 M+1 |
| `daily_clear` | 前沿日：顶已全清，本局按热图关 |
| `brush_fix_orange_acc` | 刷橙区：准度 &lt; 90% |
| `brush_fix_orange_slow` | 刷橙区：过慢（≥ 常模 mean+1σ） |
| `brush_fix_yellow` | 刷黄区：90% ≤ 准 &lt; 95% |
| `brush_fix_red` / `brush_fix_slow` | pool 内加权不足 / 准但过慢 |
| `brush_pick_mastery` | 未达热图熟练顶 |
| `brush_pick_speed` | 已熟练，相对最慢 |
| `brush_pool_empty_fallback` | 无下层可刷，退回前沿关 |

特殊模式（小数 / 平方数 / 整除等）另有各自选关，见 `category-next-levels`；教练层若推荐这些模式，先复用其关号，再决定「要不要今天练它」。

---

## 四、信号清单

v0 建议函数只许使用下列信号。案例里用到了清单外的东西，先记入「待补信号」，不要假装已经能算。

### 4.1 现在就能取

| 信号 | 来源 | 说明 |
|---|---|---|
| `grade` | `user.grade` | `null` 未填；`0` 学前；`1–12` 年级。学员端 API 不下发，report / 管理端有 |
| `daysOffline` | 上次有局之日 → 今天（中国日历） | 今天打过则为 0 |
| `daysActiveLast30` | 近 30 个中国日历日中有局的天数 | 0–30，习惯密度 |
| `lastGameTs` / 最近模式 | user + runs | 上次练的是什么 |
| 四则热图 cells | 服务端 heatmap | 每档 `active / n / p / tooSlow / fluent / timePct` 等 |
| 训练选关 | `training/next-level-debug` | `levelIndex, dayMode, frontierLevel, heatLevel, pickReason` |
| 特殊模式选关 | `category-next-levels` | 小数 / 平方数 / 整除等 |
| 各模式进度 | 学员概览 | 闯关、生存、训练、特殊模式解锁与是否通关 |
| 错题本 | `wrongAnswers` 等 | 题干、对错答案、等级、时间；**无错因标签** |
| 近局 runs | runs | 模式、对错数、时长、`trainingMeta` |

热图色带（与选关同一套，助手描述短板时用这个，不要另定颜色）：

- **橙**：准度 &lt; 90%，或过慢
- **黄**：90% ≤ 准 &lt; 95%，且未过慢
- **熟练 / 流畅**：准 ≥ 95% 且未过慢（推进梯子用）；刷热图另有「熟练顶」

### 4.2 没有或不作决策依据（v0）

| 想用的 | 实际 |
|---|---|
| 年龄 | 无生日；只用年级，未填则跳过年级规则 |
| 错题原因 | 仅有对错答案，没有教学诊断；v0 不据此改建议 |
| 家长目标、情绪、疲劳 | 无数据 |
| VIP / 管理员备注 | 可展示，默认不参与决策 |

### 4.3 派生信号

| 信号 | 计算 |
|---|---|
| `prior` | 年级 × 关号 → `learned` / `current` / `ahead` / `unknown`（§十） |
| `stage` | 热图 cell → `no_data` / `weak` / `shaky` / `thin` / `mastered` |

---

## 五、建议输出格式

程序与 report 文案都按此 JSON 形状（字段名落地时保持稳定）。

```json
{
  "ruleVersion": "0.2-provisional",
  "todayKey": "2026-09-01",
  "username": "",
  "primary": {
    "action": "training",
    "mode": "training",
    "levelIndex": 5,
    "levelLabel": "L6",
    "overwriteTrainingPick": false,
    "title": "",
    "parentCopy": "",
    "detail": ""
  },
  "alternatives": [],
  "reasons": [
    {
      "code": "",
      "ruleId": "",
      "evidence": "",
      "note": ""
    }
  ],
  "systemPick": {
    "training": { "levelIndex": 5, "dayMode": "frontier", "pickReason": "frontier_open_M1" }
  },
  "divergesFromSystemPick": false,
  "signalsUsed": {},
  "unresolved": []
}
```

`action` 预留取值（案例需要再加，不要提前用）：

| action | 含义 |
|---|---|
| `follow_training_pick` | 跟系统四则选关 |
| `training` | 四则训练（可覆盖关号 / 日模式） |
| `wrongbook` | 错题本 |
| `level` | 闯关摸底 |
| `decimal` / `perfectSquare` / `divisibility` / `expandBrackets` | 对应特殊模式 |
| `rest_or_light` | 轻量复习或今天不加压 |
| `insufficient_data` | 信号不够，只陈述现状、不硬推 |

`overwriteTrainingPick === true` 仅表示「若将来接到开局，应用本建议关号」；**v0 仍不改开局**。

原因必须能指回规则编号（如 `R03`）和一句证据（如「断练 5 天；L6 准度 82%」）。禁止只有「建议多练习」而无证据。

---

## 六、阈值表

| ID | 含义 | 值 | 标定依据 |
|---|---|---|---|
| T-ahead-n | 超前档算「会了」的最低题数 | **80（Q1 暂定）** | 案例 01 无法证伪 |
| T-floor | 绿快底板最少连续档 | **4（Q3 暂定）** | 案例 02 Amber L1–L4 |
| T-fast-pct | 底板「不算慢」 | **timePct &lt; 50（Q3）** | 案例 02 |
| T-acc-step | 准度小步 | **基线 +1.5pp，封顶 95%（Q6）** | 不是一次刷绿 |
| T-speed-ratio | 速度小步 | **任务窗均速 ≤ 基线 ×0.92，且窗内准 ≥95%（Q7）** | 对照 timePct−10；均速只用答对题。准度任务不卡速度 |
| T-fail-day | 当日封顶 | **3 局仍未成功（Q8）** | 只记**对上的那一条**未完成（1～5）。一天猛练同一关就换 |
| T-fail-days | 跨日停滞 | **3 个有练日仍未成功（Q9）** | 只记**对上的那一条**。每天练一点没进步则进冷却窗 |
| T-any-open | 碰巧完成 | **未完成 1～5（Q16）** | 非任务入口打中名单里某一关也跟局；当场成功称碰巧完成；未达也记任务窗 |
| T-abandon | 放弃局 | **整局不算** | 不进成功/失败/碰巧完成。热图仍吃已答题 |
| T-incomplete | 未完成常驻格数 | **最多 5 条，不重复（Q10）** | 黄橙优先；不够则补已学流畅（准度 × 地基加权速度 × 样本薄） |
| T-same-gap | 同 key 间隔 | **中间至少 5 条（Q11）** | 冷却窗与未完成是同一条队列；可练池空才允许更密 |
| T-done-window | 冷却窗 | **最多 5 条（Q11）** | 成功、失败都占格；满则挤最早；不再按日历日过期。规则升级、重新开单、跑偏、热图变都不清。规则升级也不拆未完成 |
| T-scan-acc | 闯关合格线 | **从 L1 起连续准≥95%，再和历史最高取 min（Q5）** | 不计分位。目标 Ln 须**通过** Ln。L1 不够 95% 时目标仍是 L1 |
| T-retry-clear | 冲榜闯关 | **预估 ≤ 纪录 × 0.85（Q15）** | 底板型已通关。预估 = 16 关均速之和 × 10 题 × 1.2。缺均速不排。不走 ≤n 前置 |
| T-foundation-floor | 绿档关掉低级加权 | **timePct &lt; 40（Q12）** | 已快则只比分位；案例 Yourself 速度档 |
| T-foundation-r | 相邻低级慢区倍率 | **1.15（Q12）** | 低 75 / 高 85 仍练低级；低 10 / 高 20 不强求 |
| T-acc-orange | 热图橙 | 准 &lt; 90% 或 timePct≥84（过慢） | 与 `heatmapCellInlineStyle` 同一套 |
| T-acc-yellow | 热图黄 | 未橙，且准 &lt; 95% **或** timePct≥50 | 与热图黄族（q≤0.5）同一套 |
| T-open-new | 安排开新关 | **已激活校内全绿** 才开一档已学/同步空关（Q14） | 热图全空只排闯关；闯关留下 n&gt;0 即当老关；超前无数据不开 |
| T-break-short | 断练「有点久」 | 待定 | |
| T-wrongbook | 错题本插入 | 待定 | |

---

## 七、决策树（v0.21）

1. **R-prior / R-stage**：同前（先验 × 阶段）。
2. **R-profile（Q3）**：从 L1 起连续「流畅且 timePct&lt;50」的档数为绿快底板。≥4 且上面还有黄/橙 → **底板型**；否则 → **平台型**。
3. **R-main-hole**：开单时队头仍是已学/同步最差黄橙（Q2）。黄橙内部排序同时看准度和 timePct。
4. **R-plan**：一条队列。**未完成最多 5 条、同一关只占一格，五条都是真任务**。成功或失败都进冷却窗（最多 5 条）。report 未完成一行五格、冷却一行五格（失败也画出来）。**不要整单重排这五格**（见下「何时动」）。
   - 开单：黄橙优先；有闯关时关号≤合格线的训练先占格，再排闯关，再排更高的洞；已激活校内全绿才加一档开新关；不够 5 条再补已学热图绿。不拿超前无数据凑格。
     - 四则两条进度线：加减 L1–L9、L12；乘除 L10、L11、L13、L14；在 L15–L16 汇合。L5 与 L6、L7 与 L8 同深。
     - 速度项：timePct&lt;40 只比分位；否则 `timePct × 1.15^本线剩余步`（剩余含汇合的 L15–L16）。黄橙排序仍用未加权 timePct。
     - 绿档小步：准度未到 97% 走准度（+1.5pp、封顶 97%）；准度已 ≥97%、过慢或 timePct≥50 走速度小步。速度小步成功还须任务窗准度 ≥95%。禁止目标准度低于当前热图准度。
   - **同 key 间隔（Q11）**：两个相同任务之间默认空 5 条（冷却窗+排队一起数）。闯关也走这条，不另做日历冷却。
   - **闯关合格线（Q5）**：从 L1 起连续准度 ≥95%（不管分位），再和历史闯关最高取 min。目标 **Ln 须通过 Ln**（摸到该关不算）。L1 不够 95% 时仍排闯关，目标为 L1。
   - **闯关位置**：开单 / 重新开单 / 跑偏（且没有「已有尝试」的关挡着）时，未完成里关号 ≤ n 的训练排在闯关前面；刚在冷却窗里出现过的关不再挡路。成功/失败只补空位时**不**按最新 n 把剩下四格重排。
   - **冲榜闯关（Q15）**：底板型已通关时，用热图每关均速估全通用时（×10 题 ×1.2）。预估 ≤ 纪录 85% 才再排，目标须通关 L16。**不**把全部训练排在闯关前；有黄橙时占第二格，全绿则队头。间隔 5 仍有效。
   - **破例**：按热图该练的（黄橙、开新关、值得补的绿、闯关）去掉间隔不够的之后，**一条都排不出来**，才允许提前把刚做过的关再排进来。有别的可练时不靠重复凑满 5 格。
   - **队头（任务 1）**：推荐先练（描边）。跟这条走时把局数记进任务窗。达标 → 进冷却（成功），**只补一格，剩下四格原样不动**。当天 3 局或 3 个有练日仍未达标 → 失败、进冷却、**不后插**、窗清零；同样只补一格。未达成功也未失败则人不动、不重算、不补格。点任务 2～5 开练**不**把该条抽到队头。
   - **排队（任务 2～5）**：与队头同一套跟局：记任务窗、未达可再试、成功/失败进冷却并只补一格。不再要求「这一局当场达标才认」。
   - **碰巧（非任务入口，Q16）**：首页或其它模式打的局，只要关号/模式对上未完成 1～5 里某一条，就跟那一条（未达也记窗）。这一局让该条成功了，称**碰巧完成**。对不上 1～5 才是跑偏。
   - **放弃局**：中途放弃（`abandoned` 或 `trainingMeta.abandoned`）整局不算成功、不算失败、也不碰巧完成。热图仍计入该局已答的题。训练日模式仍不把放弃当打完一局。
   - **跑偏（1～5 以外）**：跟完这一局后才按最新热图重算未完成。冷却窗保留。**已经有过尝试记录**（任务窗 games&gt;0 或 items&gt;0）的关保留任务窗、不扔掉重开；没有尝试记录的格子可以换成热图现在该练的。
   - **规则升级**：先用**旧五格**把积压局跟完。升级只改版本号 / 文案，**不拆队、不重排未完成**。打开名单本身也不因为升了版本就重开五格。
   - **重新开单**（report 按钮）：丢掉未完成进度，按热图重排五格。冷却窗保留（满 5 才挤最早）。`lastProcessedTs` 不重置，旧局不重跟。换账号或无存盘才整单新开。全量成功历史只记成功，失败只进冷却窗。
5. **R-ahead-nodata**：超前无数据仍禁止开。
6. **R-open-new（Q14）**：四则热图全空（无已激活格）时**只排闯关**，目标 L1，不排开新关。已学/同步里已激活的关全部热图绿，才安排一档开新关（关号最小的空档）。成功：1 局或 20 题即激活。闯关或其它模式一旦留下 n&gt;0，该档按老关走黄橙/绿，不再发「开新关」任务。

冲突：

| 日期 | 案例 | 与哪条规则冲突 | 裁定 |
|---|---|---|---|
| 2026-09-02 | 案例 03 johnnyhe | 助手只按准度判绿，热图按准×速上色 | **已改 v0.8**：黄橙对齐热图。L10 分位 83 为黄洞，排在刚黄 L11 前。 |
| 2026-09-05 | 自己账号 L4 任务局 | 规则升级先拆五格再跟局，对不上判跑偏 | **已改 v0.20**：积压局先对旧五格跟完；升级不拆队。 |

---

## 八、案例怎么写

尽量包含四块。缺的我可以帮你从数据补。

```
### 案例 X · 一句话标题（如「断练后不要冲关」）

- 学员：用户名 或 虚拟画像
- 你看到的情况：（年级 / 多久没练 / 热图哪几档差 / 孩子实际感受……）
- 系统默认选关：（不知道就写「未知」，我补）
- 你的主建议：模式 + 关号或「跟系统」+ 大概练多久/几局（若有）
- 原因：（按重要性排列）
- 明确不要：今天不要做什么
- 若情况再变一点：例如「如果只断练 1 天呢？」
```

标定完成后，每个案例补一行「已收入规则：R0x」。

---

## 九、案例记录

下面先留空槽。新案例追加编号，不要改已经收入规则的旧案例正文；要改结论就在案例下加「修订」。

### 案例 01 · Yourself：三年级 9 月不应开 L14

全文见《练习建议案例.md》。已收入分型 + 混合任务单（平台型）。v0.9 开新关：有黄橙则不开 L14。

### 案例 02 · Amber：五年级，L14 已是已学

全文见《练习建议案例.md》。已收入分型 + 混合任务单（底板型）；弱项顺序待主管改。

### 案例 03 · johnnyhe：L10 极慢排在刚黄 L11 后

全文见《练习建议案例.md》。根因是助手黄橙与热图上色不一致。**v0.8 已对齐**；L10 进黄洞，排在 L11 前。

---

## 十、可执行规则（v0）

实现：`server/practice-advice.js` 的 `computePracticeAdvice`（只在服务器跑，不进 H5）。只改这一份。

### 校内先验（不管月份）

| 年级 | 已学 | 同步 | 超前 |
|---|---|---|---|
| 未填 | （不分类，只打热图短板） | | 不挡 |
| 学前 | — | L1–L4 | L5+ |
| 1 | L1–L6 | L7–L9 | L10+ |
| 2 | L1–L9、L12 | L10–L11 | L13+ |
| 3 | L1–L12 | — | L13–L16 |
| 4 | L1–L14 | L15–L16 | — |
| 5–12 | L1–L16 | — | — |

### 表现阶段

- 无数据：未激活或 n=0
- 不会/很生：橙（准 &lt; 90% 或 timePct≥84）
- 不熟 / 会但偏慢：黄（未橙，且准 &lt; 95% 或 timePct≥50）
- 样本薄：超前且热图流畅但 n &lt; 80（Q1 暂定）
- 会了：流畅（准 ≥95% 且 timePct&lt;50）；超前还要 n≥80

### 未完成何时动（v0.21，勿忘）

**原则：不要整单重排未完成五格。** 2026-09-05 事故：0.18→0.19 先按新热图拆掉五格，再拿刚打的 L4 任务局去对，对不上判跑偏；L4 从名单消失，冷却和成功历史都没有，结算既不是成功也不是再试。

| 时机 | 未完成怎么动 | 冷却 |
|---|---|---|
| **任务局**（从某一条的「开始挑战」进） | 只跟这一条。未完成**不**因这一局整单重算，也不把该条抽到队头。成功或失败少一格，才在队尾**补进一格**；剩下四格原样不动。未达成功也未失败：人不动、不补格。 | 成功/失败进冷却；未达不进 |
| **碰巧**（没用任务按钮，但打中未完成 1～5 里某一条） | 与任务局同一套跟局：记任务窗；当场成功称碰巧完成。 | 同左 |
| **非任务局**（打到名单外） | 跟完这一局后才因热图变了重算未完成。已有尝试记录的关保留任务窗；没练过的格子可以按热图换成别的。 | 保留 |
| **规则升级** | 先用旧五格把积压局跟完。只改 `ruleVersion` / 文案，**不拆队**。打开名单也不因为升了版本就重开五格。 | 保留 |
| **重新开单** | 丢掉未完成进度，按热图重排五格（闯关前置等开单逻辑在这里跑）。 | 保留 |
| **无存盘 / 换账号** | 整单新开。 | — |

跟局顺序：积压局必须对着**升级前的旧未完成**匹配。禁止「先按新规则拆五格，再拿本局去对」。

### 开任务单（v0.21）

未完成最多 **5** 条、不重复。**1～5 都能开练**，对上哪条跟哪条（成功/失败/未达）。首页打中名单里的关也跟局；当场成功称碰巧完成。冷却窗最多 **5** 条，**成功和失败都占格**；满 5 条挤最早，**不再按日历日过期**。失败从未完成拿走、不后插。规则升级、重新开单、跑偏、热图变都**不清**冷却窗。规则升级也**不拆**未完成。report 两行各五个方块（冷却行画出失败）。全量历史只记成功，与冷却分表。

同一 `key`（训练某一关，或闯关）两次出现之间，中间至少再排 **5** 个别的任务。可练池空（没有黄橙、没有值得补的绿、也不能开新关）才允许更密。

黄橙与热图格子同一套（Q13）：准或速度任一不够，就算洞。内部排序仍用未加权 timePct + 准度。

**开新关（Q14，严格）：** **热图全空只排闯关**（目标 L1），不排开新关。已学/同步里**已经激活**的关全部是热图绿时，才加入一档「训练 Lx（开新关）」= 已学或同步里关号最小、仍无数据的档。一次最多 1 条，不和黄橙抢队头（有黄橙就说明未全绿，根本不会排）。超前无数据不开。成功：打满 **1 局或 20 题**即激活，不要求一次 97%。例外：闯关（或其它模式）意外打到新关并留下数据后，该档自动变老关，按热图黄橙/绿参与排序，不再单独安排开新关。

不够 5 条时，在已学**热图绿**（准≥95% 且 timePct&lt;50）里按必要性补：准度离 97% 越远、**地基加权速度项**越大、样本越薄，越先补。

绿档速度项（Q12）：

- 加减线：L1 → L2 → L3 → L4 → L5/L6 → L7/L8 → L9 → L12；乘除线：L10 → L11 → L13 → L14；汇合 L15 → L16。
- `timePct < 40`：速度项 = timePct（已快，不按低级加权）。
- 否则：速度项 = `timePct × 1.15^剩余步`。剩余步 = 本线后面档数 + 混合 2 档（L15、L16）；混合档自身不含「再加两条线」。

绿档成功线：准度 &lt; 97% 用准度小步（+1.5pp、封顶 97%）；准度已 ≥97%、过慢或 timePct≥50 用速度小步。速度小步：任务窗均速达标 **且** 窗内准度 ≥95%（防止只冲速度不顾正确率）。准度小步不卡速度。不准写出低于当前准度的目标。

- **平台型**：开单时：关号≤闯关目标的短板 → 闯关（须过 Ln）→ 其余黄橙 →（全绿时开新关）→ 已学流畅补格。成功后不整单按新 n 重排，只补空位。
- **底板型**：开单时短板差→好。已通关默认不插摸底闯关；若热图预估全通 ≤ 纪录 85%，再排冲榜闯关（须通关 L16，不走 ≤n 前置）。全绿时同样可开新关。

任务单（未完成 + 冷却窗）保存在服务器（按用户名）。H5 不跑引擎。全量成功历史只记成功、程序不删。点「重新开单」只重算未完成，冷却窗保留。未完成 1～5 对上就跟局，未达不重算；打列表外才重算未完成（已有尝试的关保留）。算单发生在打开每日任务、report 点「刷新」、重新开单；report 打开默认只读快照。任务局结算会当场跟这一条，但**不**因此重开另外四格。**中途放弃的局整局不算**（不进成功/失败/碰巧完成）；热图仍吃该局已答题。速度小步与任务窗均速只算答对题。速度小步成功还须窗内准度 ≥95%。速度任务成功还须至少 **2 局或 20 题**（一局达标只是未达、可再试，不会进冷却）。

### Q1–Q3、Q5–Q16（暂定）

Q4 已废。程序输出会列出其余暂定。

---

## 十一、落地计划

1. **v0（进行中）：** report 数据分析顶部展示助手建议，与旧选关并排对照。
2. **磨规则：** 主管判断与程序矛盾时改 §十，不要改训练 F/H。
3. **v1 / v2：** 主页角色；再用助手取代训练开局。

---

## 十二、待决

- 一天主建议：v0.14 队头计失败。v0.16 规则升级/重新开单不清冷却窗。v0.17 放弃局整局不算；均速只算对题。v0.18 速度任务还须窗内准度 ≥95%。v0.19 失败进冷却窗、不后插；report 冷却行画出失败。v0.20 不整单重排未完成。v0.21 未完成 1～5 都能开练、对上就跟局；首页碰巧也记未达；碰巧完成 = 非任务入口当场成功。
- 训练之外（错题本、小数等）尚未纳入。
- 「今日练习」局数/时长上限未定。
- 先验是否随学年月份放宽未定。

---

*规则版本：`0.21-provisional`。*
