# Jarvis Math Lab · 成就系统说明

> **读者**：产品/教研定稿；实现见 `server/achievements/`（判定引擎）、`data/achievements-catalog.json`（展示与奖励配置）、管理页「成就管理」Tab。  
> **本文即唯一规格**：改成就先改本文 §4，再同步 catalog / 代码（或双向 diff）。

---

## 1. 文档怎么用

| 部分 | 内容 | 谁改 |
|------|------|------|
| **§2～§3** | 架构、分两阶段落地、已拍板规则 | 开发 / 产品 |
| **§4** | 成就清单（名称、XP、判定、规则类型） | **你主要改这里** |
| **§5** | 规则类型注册表（代码能力边界） | 开发；新增类型需发版 |
| **§6** | 数据字段与 API 草案 | 开发 |

改 §4 时注意：

- **`id` 一旦上线勿改**（学员已解锁会写进 `users.json`）。
- 改 **名称 / icon / XP / 排序 / 启用** 只动 catalog，不必改判定代码（规则类型与参数不变时）。
- 改 **判定条件** 需改 `ruleType` 或 `ruleParams`，并确认是否对老学员 **retroactive 重算**。

---

## 2. 管理页 vs 代码：怎么分工（核心问题）

管理页 **不能** 写任意 JavaScript 判定逻辑，否则无法保证安全、可测试、与 runs 一致。  
做法是 **「Catalog 配置 + Rule 引擎」** 两层：

```text
┌─────────────────────────────────────────────────────────┐
│  data/achievements-catalog.json（管理页可编辑）            │
│  id, name, icon, xpReward, category, hint, enabled, …   │
│  ruleType + ruleParams（只能选 §5 已注册的类型）           │
└──────────────────────────┬──────────────────────────────┘
                           │ id + ruleType + ruleParams
                           ▼
┌─────────────────────────────────────────────────────────┐
│  server/achievements/evaluators.js（代码，发版更新）       │
│  每种 ruleType 一个通用判定函数，参数来自 catalog          │
└──────────────────────────┬──────────────────────────────┘
                           │ 读 runs + users
                           ▼
┌─────────────────────────────────────────────────────────┐
│  users[].achievements[id] = unlockedAt                  │
│  users[].equippedBadges = [id, id, id]（最多 3）        │
│  解锁时 totalScore += xpReward（仅一次）                  │
└─────────────────────────────────────────────────────────┘
```

### 管理页能做什么

| 操作 | 管理页 | 需要发版 |
|------|--------|----------|
| 改成就 **名称、icon、说明、XP、排序、启用** | ✅ | 否 |
| 用 **import 文件** 增量添加成就（新 id） | ✅ | 需发版 import API；文件随 Git |
| 改 **ruleParams**（如 L8→L10） | ✅ | 建议 retroactive 重算 |
| 发明 **全新判定逻辑**（§5 没有的类型） | ❌ | 是（先加 ruleType） |
| 改 **id**（已上线成就） | ❌ | 应新建 id，旧 id 废弃 |

### 学员端 / 成就墙

- 文案 **直接读 catalog**（不走 i18n 包）；繁中/英文若以后要支持，可在管理页加 `nameEn` 字段，v1 仅中文即可。
- **点亮 / 进度** = 引擎对 catalog 里每条 `enabled` 成就跑 evaluator；未解锁显示 `hint` 模板 + 进度（若该 ruleType 支持）。

### 排行榜徽章

- 榜接口返回 `equippedBadges: [{ id, icon, name }]`（最多 3），来自 user + catalog。
- **荣誉类**（进榜）解锁后 **永久保留**（已拍板），掉榜不收回。

### 发布新成就（import 文件 → 管理页上传）

线上 catalog 在 Render **`DATA_DIR/achievements-catalog.json`**，**不会**随 Git push 自动更新。新增成就流程：

1. 开发在 `server/achievements/imports/` 添加 **`jml-achievement-import`** 格式 JSON（仅 `addItems` / `addCategories`，不全量覆盖）。
2. 发版 server + 管理端（含「从文件添加」按钮）。
3. 管理页 → 成就 Tab → **从文件添加** → 选文件 → **预览**（`dryRun`）→ **确认添加** → 写入线上 catalog。
4. 在列表里逐条 **上传徽章图**；按需 **全量重算成就**。

**合并规则**：`addItems[].id` 已存在 → **跳过并写入报告**；`addCategories` slug 已存在 → 跳过；`appendCategoryOrder` 只追加未出现的 slug。

示例文件：`server/achievements/imports/2026-05-19-getting-started-honor.json`（含 `zero_wrong_clear_once`、`streak_3`、`rank_any_first`）。

```json
{
  "schema": "jml-achievement-import",
  "version": 1,
  "comment": "说明文字",
  "addCategories": { "honor": { "name": "荣誉", "nameEn": "Honor" } },
  "appendCategoryOrder": ["honor"],
  "addItems": [ { "id": "...", "ruleType": "...", "ruleParams": {} } ]
}
```

---

## 3. 分两阶段实施

### 阶段 A：接好功能（不依赖 §4 定稿）

1. **数据**：`achievements-catalog.json` 初始 2～3 条测试成就；`users` 增加 `achievements`、`equippedBadges`。
2. **引擎**：记局（非 comboOnly）/ 管理页「重算成就」时调用 `evaluateUserAchievements`。
3. **API**：GET catalog；GET/PATCH 用户成就与佩戴；管理员 CRUD catalog。
4. **管理页**：成就管理 Tab（列表编辑 name/icon/xp/ruleType/ruleParams/enabled；**ruleType 下拉仅 §5 已注册项**）。
5. **学员端**：成就墙 UI（分类、已解锁/未解锁、进度条、佩戴 3 枚）；首页头像旁展示 3 徽章；排行榜昵称旁展示；解锁 toast。
6. **成就墙**：对已登录学员开放；游客需先登录。

### 阶段 B：按你改好的 §4 补全成就

1. 根据 §4 表格批量写入 / 导入 `achievements-catalog.json`。
2. 缺 ruleType 的补 evaluator（应尽量少）。
3. 全量 **retroactive** 扫 `runs.json` + `users.json` 一次。
4. 成就墙已对全部登录学员开放（2026-05 起）。

---

## 4. 已拍板的产品规则

| # | 问题 | 决定 |
|---|------|------|
| 1 | 排行榜类成就掉榜是否收回 | **不收回**，解锁永久保留 |
| 2 | 零失误类成就数量 | **你先改 §4**；初稿按下面表格 |
| 3 | 成就 XP | **一次性小额 XP**，数值在 **管理页 / catalog** 配置，解锁时加进 `totalScore` |
| 4 | 学员数据页展示成就 | **暂不做** |
| 5 | 文案与 i18n | **管理页改 catalog**，不走 `i18n.json` |

---

## 5. 规则类型注册表（代码能力，v1）

管理页新增成就时，**只能**选用下表类型；参数写在 catalog 的 `ruleParams`（JSON）。

| ruleType | 含义 | ruleParams 示例 | 进度是否可显示 |
|----------|------|-----------------|----------------|
| `any_run` | 任意模式完成局数 | `{ "minCount": 1 }` | 是（当前/目标局数） |
| `any_zero_wrong_clear` | 任意模式 0 错完成至少 1 局 | `{}` | 否（一次性） |
| `mode_run_count` | 指定 mode 局数 | `{ "mode": "training", "minCount": 30 }` | 是 |
| `distinct_modes` | 不同 mode 各至少 1 局 | `{ "minModes": 3, "modes": ["training","level",…] }` | 是 |
| `level_challenge_best` | 闯关最高关 | `{ "minLevelIndex": 15 }` 表示 L16 | 是（当前 best） |
| `level_perfect_run` | 闯关某关及以下整局 0 错通关 | `{ "maxLevelIndex": 7, "mode": "level" }` | 否（一次性） |
| `training_heatmap_pass` | 训练热图某关达标 | `{ "levelIndex": 15, "minPct": 95 }` | 是（当前 pct） |
| `wrongbook_cleared` | 错题练习清空错题本 | `{}` | 否 |
| `survival_unlocked` | 已解锁生存模式 | `{}` | 否 |
| `survival_cleared` | 生存通关至少 1 次 | `{ "minCount": 1 }` | 否 |
| `survival_run_best` | 生存单局指标 | `{ "minSeconds": 300 }` 或 `{ "minMaxLevelIndex": 9 }` | 是 |
| `decimal_best_level` | 小数最高解锁/完成 | `{ "minLevelIndex": 4 }` 表示 D5 | 是 |
| `expand_unlock_level` | 拆括号解锁等级 | `{ "minLevelIndex": 4 }` 表示 L5 | 是 |
| `expand_perfect_run` | 拆括号某级 20 题全对 | `{ "levelIndex": 2 }` | 否 |
| `perfect_square_unlock` | 平方数解锁等级 | `{ "minLevelIndex": 2 }` 表示 L3 | 是 |
| `perfect_square_perfect_run` | 平方数某级 0 错 | `{ "levelIndex": 1 }` | 否 |
| `prime_perfect_run` | 质数 50 题全对 | `{ "questionCount": 50 }` | 否 |
| `prime_run_count` | 质数模式局数 | `{ "minCount": 10 }` | 是 |
| `streak_best` | 最长连续挑战天（`user.streakBest`） | `{ "minDays": 7 }` | 是 |
| `combo_best` | 最高连对 | `{ "minCombo": 20 }` | 是 |
| `player_level` | 玩家等级（由 totalScore 推算） | `{ "minLevel": 10 }` | 是 |
| `ranking_any_top_n` | 任意排行榜名次 ≤ N（仅看当前排名，不回溯历史） | `{ "maxRank": 1 }` | 否 |
| `ranking_top_n` | 某榜前 N（解锁后不收回） | `{ "board": "survival", "maxRank": 50 }` | 否 |

`board` 取值：`score` | `survival` | `primePerfect` | `streak` | `combo`。

**`any_zero_wrong_clear` 判定**（runs 入库局，`comboOnly !== true`）：

- `wrongCount === 0`
- 非放弃：`abandoned !== true`（闯关放弃写入此字段；训练放弃同时写 `trainingMeta.abandoned`）
- 生存模式须 `cleared === true`（排除倒计时结束）

**`ranking_any_top_n` 判定**（与 5 个排行榜 API 排序一致；**仅当前名次**，不做历史回溯）：

- `board` 覆盖：等级榜 `score`、生存 `survival`、质数达人 `primePerfect`、耐力 `streak`、连击 `combo`
- 任一榜 `rank <= maxRank`（如 `maxRank: 1` 即榜一）即满足
- 解锁后掉榜不收回

---

## 6. 成就清单（可编辑）

说明：

- **levelIndex** 均为 **0 起算**（L1 = 0，L16 = 15；D1 = 0，D5 = 4）。
- **XP** 为初稿，请直接在表中改；上线后在管理页改 catalog 即可。
- **icon** 建议 v1 用 emoji 或 `pictures/achievements/` 下文件名；管理页可换。
- **enabled**：`true` 表示上线；开发阶段可设 `false`。

### A. 新手任务

| id | 名称 | icon | 类别 | 稀有度 | XP | enabled | ruleType | ruleParams | hint（未解锁提示） |
|----|------|------|------|--------|-----|---------|----------|------------|-------------------|
| `first_run` | 初次挑战 | 🎯 | 新手任务 | 铜 | 10 | true | `any_run` | `{ "minCount": 1 }` | 完成任意 1 局挑战 |
| `zero_wrong_clear_once` | 零失误初体验 | 💯 | 新手任务 | 铜 | 15 | true | `any_zero_wrong_clear` | `{}` | 任意模式 0 错完成 1 局 |
| `streak_3` | 三日打卡 | 📅 | 新手任务 | 铜 | 15 | true | `streak_best` | `{ "minDays": 3 }` | 最长连续挑战天 ≥3 |
| `runs_50` | 勤练之星 | ⭐ | 新手任务 | 银 | 30 | false | `any_run` | `{ "minCount": 50 }` | 累计完成 50 局 |

### A2. 荣誉

| id | 名称 | icon | 类别 | 稀有度 | XP | enabled | ruleType | ruleParams | hint |
|----|------|------|------|--------|-----|---------|----------|------------|------|
| `rank_any_first` | 榜一学员 | 👑 | 荣誉 | 金 | 50 | true | `ranking_any_top_n` | `{ "maxRank": 1 }` | 在任意排行榜中拿到第 1 名 |

### B. 整数四则 · 闯关 / 训练

| id | 名称 | icon | 类别 | 稀有度 | XP | enabled | ruleType | ruleParams | hint |
|----|------|------|------|--------|-----|---------|----------|------------|------|
| `level_pass_l4` | 两位数入门 | 4️⃣ | 闯关 | 铜 | 15 | true | `level_challenge_best` | `{ "minLevelIndex": 3 }` | 闯关通过 L4 |
| `level_pass_l8` | 进退位能手 | 8️⃣ | 闯关 | 银 | 25 | true | `level_challenge_best` | `{ "minLevelIndex": 7 }` | 闯关通过 L8 |
| `level_pass_l12` | 乘法进阶 | 🔢 | 闯关 | 银 | 35 | true | `level_challenge_best` | `{ "minLevelIndex": 11 }` | 闯关通过 L12 |
| `level_pass_l16` | 四则大师 | 👑 | 闯关 | 金 | 80 | true | `level_challenge_best` | `{ "minLevelIndex": 15 }` | 闯关通关 L16 |
| `level_perfect_l8` | L8 零失误 | 💎 | 闯关 | 金 | 40 | true | `level_perfect_run` | `{ "maxLevelIndex": 7, "mode": "level" }` | 闯关在 L8 及以下整局 0 错通关 |
| `training_l16_heatmap` | L16 热图达标 | 🔥 | 闯关 | 金 | 60 | true | `training_heatmap_pass` | `{ "levelIndex": 15, "minPct": 95 }` | 训练模式 L16 热图正确率 ≥95% |
| `training_30_runs` | 训练常客 | 📗 | 闯关 | 银 | 30 | true | `mode_run_count` | `{ "mode": "training", "minCount": 30 }` | 训练模式累计 30 局 |
| `wrongbook_clear_once` | 错题清零 | 🧹 | 闯关 | 银 | 25 | true | `wrongbook_cleared` | `{}` | 错题练习一局全对并清空错题本 |

### C. 生存模式

| id | 名称 | icon | 类别 | 稀有度 | XP | enabled | ruleType | ruleParams | hint |
|----|------|------|------|--------|-----|---------|----------|------------|------|
| `survival_unlock` | 生存解锁 | 🔓 | 生存 | 铜 | 10 | true | `survival_unlocked` | `{}` | 解锁生存模式 |
| `survival_first_clear` | 生存通关 | 🏁 | 生存 | 金 | 50 | true | `survival_cleared` | `{ "minCount": 1 }` | 生存模式首次通关 |
| `survival_time_300` | 坚持 5 分钟 | ⏱️ | 生存 | 银 | 30 | true | `survival_run_best` | `{ "minSeconds": 300 }` | 单局生存时间 ≥5 分钟 |
| `survival_reach_l10` | 生存 L10 | 🎚️ | 生存 | 银 | 35 | true | `survival_run_best` | `{ "minMaxLevelIndex": 9 }` | 单局生存最高到达 L10 |

### D. 专项模式

| id | 名称 | icon | 类别 | 稀有度 | XP | enabled | ruleType | ruleParams | hint |
|----|------|------|------|--------|-----|---------|----------|------------|------|
| `decimal_d3` | 小数进阶 | ·3 | 专项 | 铜 | 20 | true | `decimal_best_level` | `{ "minLevelIndex": 2 }` | 小数模式达到 D3 并完成 1 局 |
| `decimal_d5` | 小数毕业 | ·5 | 专项 | 金 | 50 | true | `decimal_best_level` | `{ "minLevelIndex": 4 }` | 小数模式达到 D5 并完成 1 局 |
| `expand_l3` | 拆括号 L3 | 📐 | 专项 | 铜 | 20 | true | `expand_unlock_level` | `{ "minLevelIndex": 2 }` | 拆括号进度到 L3 |
| `expand_l5` | 拆括号毕业 | 📏 | 专项 | 金 | 60 | true | `expand_unlock_level` | `{ "minLevelIndex": 4 }` | 拆括号解锁 L5 |
| `expand_perfect_l3` | 拆括号满分 | ✨ | 专项 | 金 | 40 | true | `expand_perfect_run` | `{ "levelIndex": 2 }` | 拆括号 L3 整局 20 题全对 |
| `ps_l3_unlock` | 平方数 L3 | ▪️ | 专项 | 银 | 35 | true | `perfect_square_unlock` | `{ "minLevelIndex": 2 }` | 平方数解锁 L3 |
| `ps_perfect_l2` | 平方数完美 | ◻️ | 专项 | 银 | 30 | true | `perfect_square_perfect_run` | `{ "levelIndex": 1 }` | 平方数 L2 整局 0 错 |
| `prime_perfect_50` | 质数达人 | 🧠 | 专项 | 金 | 50 | true | `prime_perfect_run` | `{ "questionCount": 50 }` | 质数合数 50 题全对完成 1 局 |
| `prime_runs_10` | 质数常客 | 🔬 | 专项 | 铜 | 20 | true | `prime_run_count` | `{ "minCount": 10 }` | 质数模式累计 10 局 |

### E. 习惯与坚持

| id | 名称 | icon | 类别 | 稀有度 | XP | enabled | ruleType | ruleParams | hint |
|----|------|------|------|--------|-----|---------|----------|------------|------|
| `streak_7` | 一周不断 | 📆 | 习惯 | 银 | 30 | true | `streak_best` | `{ "minDays": 7 }` | 最长连续挑战天 ≥7 |
| `streak_30` | 月度坚持 | 🗓️ | 习惯 | 金 | 100 | true | `streak_best` | `{ "minDays": 30 }` | 最长连续挑战天 ≥30 |
| `combo_10` | 十连对 | 🔗 | 习惯 | 铜 | 15 | true | `combo_best` | `{ "minCombo": 10 }` | 最高连对 ≥10 |
| `combo_20` | 二十连对 | ⛓️ | 习惯 | 银 | 30 | true | `combo_best` | `{ "minCombo": 20 }` | 最高连对 ≥20 |

> **v2 预留**：`weekly_5runs`（自然周 ≥5 局）需新增 ruleType `weekly_run_count`，首版未列入。

### F. 等级与荣誉

| id | 名称 | icon | 类别 | 稀有度 | XP | enabled | ruleType | ruleParams | hint |
|----|------|------|------|--------|-----|---------|----------|------------|------|
| `player_lv_5` | 学员 Lv.5 | 🥉 | 等级 | 铜 | 20 | true | `player_level` | `{ "minLevel": 5 }` | 玩家等级达到 Lv.5 |
| `player_lv_10` | 学员 Lv.10 | 🥈 | 等级 | 银 | 40 | true | `player_level` | `{ "minLevel": 10 }` | 玩家等级达到 Lv.10 |
| `player_lv_25` | 资深学员 | 🥇 | 等级 | 金 | 80 | true | `player_level` | `{ "minLevel": 25 }` | 玩家等级达到 Lv.25 |
| `rank_score_top50` | 等级榜前 50 | 🏆 | 荣誉 | 金 | 30 | true | `ranking_top_n` | `{ "board": "score", "maxRank": 50 }` | 进入等级榜前 50 |
| `rank_survival_top50` | 生存榜前 50 | 🛡️ | 荣誉 | 金 | 30 | true | `ranking_top_n` | `{ "board": "survival", "maxRank": 50 }` | 进入生存榜前 50 |
| `rank_streak_top50` | 耐力榜前 50 | 💪 | 荣誉 | 金 | 30 | true | `ranking_top_n` | `{ "board": "streak", "maxRank": 50 }` | 进入耐力榜前 50 |

**合计：37 条**（v1 可先启用子集，其余 `enabled: false`）。

---

## 7. 数据字段与 API 草案

### users.json 新增字段

```json
{
  "achievements": {
    "first_run": 1716000000000
  },
  "equippedBadges": ["level_pass_l16", "streak_7", "survival_first_clear"]
}
```

- `achievements`：键为成就 `id`，值为解锁时间戳（ms）。
- `equippedBadges`：最多 3 个 id，顺序即展示顺序。

### achievements-catalog.json（管理页维护）

```json
{
  "version": 1,
  "items": [
    {
      "id": "first_run",
      "name": "初次挑战",
      "icon": "🎯",
      "category": "入门",
      "tier": "铜",
      "xpReward": 10,
      "hint": "完成任意 1 局挑战",
      "ruleType": "any_run",
      "ruleParams": { "minCount": 1 },
      "sortOrder": 10,
      "enabled": true
    }
  ]
}
```

### API（草案）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/achievements/catalog` | 学员可读；返回 enabled 项 |
| GET | `/api/user/:username/achievements` | 本人成就 + 进度 + equipped |
| PUT | `/api/user/:username/achievements/equipped` | 本人设置 3 枚佩戴 |
| GET | `/api/admin/achievements/catalog` | 管理：全量含 disabled |
| PUT | `/api/admin/achievements/catalog` | 管理：保存 catalog |
| POST | `/api/admin/achievements/import` | 管理：增量导入（`{ import, dryRun: true\|false }`） |
| POST | `/api/admin/achievements/recompute` | 管理：全量或指定用户 retroactive |

排行榜各接口在 user 条目中附带 `equippedBadges` 摘要（icon + name，供渲染）。

---

## 8. 阶段 A 实施前还需你确认的事项

| # | 事项 | 建议默认 | 你的决定 |
|---|------|----------|----------|
| 1 | 成就墙何时对全体学员开放 | **已开放**（登录学员；游客需先登录） | 已决定 |
| 2 | 解锁 toast 是否每局最多弹 1 条 | 是，避免连弹 | |
| 3 | retroactive 是否阶段 A 就做 | 是，至少支持管理员手动触发 | |
| 4 | icon 存储 | v1 emoji；可选上传图到 `pictures/achievements/` | |
| 5 | 佩戴位不足时新解锁 | 只进收藏，不自动替换已佩戴 | |
| 6 | catalog 备份 | 纳入现有系统备份 Tab | |
| 7 | `distinct_modes` 计数的 mode 列表 | 见下表 | |

**计入「不同模式」的 mode 值**（与 runs.mode 一致）：

`training` · `level` · `survival` · `decimal` · `expandBrackets` · `perfectSquare` · `primeComposite`

（`wrong` 错题练习局是否计入：**建议不计**，避免刷成就。）

---

## 9. 附录：实现文件规划（阶段 A）

| 路径 | 职责 |
|------|------|
| `data/achievements-catalog.json` | 本地规格参考；线上以 DATA_DIR 为准 |
| `server/achievements/evaluators.js` | ruleType → 判定函数 |
| `server/achievements/import.js` | import 合并与报告 |
| `server/achievements/imports/*.json` | 待上传的增量成就文件 |
| `server/achievements/engine.js` | 遍历 catalog、写 unlock、发 XP |
| `server/server.js` | 挂 API；append run 后 hook |
| `docs/admin/` | 成就管理 Tab |
| `docs/index.html` | 成就墙、佩戴 UI、排行榜徽章 |
| `成就系统说明.md` | **本文** |

改完 §4 后把文件存盘，阶段 B 按表导入 catalog 并跑 retroactive 即可。
